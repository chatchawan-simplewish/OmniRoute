# Continue OmniRoute secure-console and standard-team API goal

## Ownership transfer

The prior task `Resume OmniRoute secure-console goal`
(`01a05d2e-e88d-7113-acb2-4483f99edfb7`) stopped unarchived at the project
85% context boundary. Under project-root `AGENTS.md` session-continuity rules,
the new continuation task receives exclusive write and authority-gate ownership
for this worktree and the OmniRoute secure-console/Cloudflare token-object lane.
The prior task must not resume modifying work or consume any gate.

Worktree:
`C:\ChatGPT Projects\SW-Selfhosted-Network\.worktrees\omniroute-agent-routing-source`

Branch: `codex/omniroute-agent-routing`

Safe-pause handoff parent: `be427102721e31d86a95d2a0309e46bea3dbb71a`.
The handoff commit must directly follow that parent and change only
`.planning/HANDOFF.json` and `.planning/.continue-here.md`.

## Required Reading — exact order

Read and acknowledge every blocking constraint before acting:

1. `C:\ChatGPT Projects\SW-Selfhosted-Network\AGENTS.md`
2. `.planning/HANDOFF.json`
3. this `.planning/.continue-here.md`
4. `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-secure-console-transfer-v49-fresh-realm-token-page-readiness-live-result.md`
5. `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-secure-console-transfer-v50-unique-url-token-page-reacquisition-design.md`
6. `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-secure-console-transfer-v50-unique-url-token-page-reacquisition-design-sol-review.md`
7. `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-secure-console-transfer-v50-unique-url-token-page-reacquisition-design-fix1-sol-review.md`
8. V49 implementation lineage: its final brief, executable, pure fixture,
   fix1 review package, and fix1 Sol review in the same phase directory
9. V48 diagnostic live result and the V4 replacement brief/PASS review as
   static evidence only

## Blocking constraints

- V3 is consumed and failed. Never retry, continue, reuse, reinterpret, or
  relax it.
- V4 is static evidence only. Never execute it directly; its persistent
  bindings do not cross tasks.
- V35-V42, V44, V46, V47, V48, and V49 are spent. V43 is permanently
  ineligible. Never retry, continue, reuse, reinterpret, or relax any of them.
- V49 stopped after exactly one `openTabs` call and before claim. Its exact
  rejecting property remains **NOT PROVEN**. Do not inspect or reconstruct the
  raw listing as if V49 authorized diagnosis.
- V49's single fixed state-only cleanup proof passed and the CUA realm was
  reset. No browser binding exists for the continuation.
- Do not initialize CUA, list or inspect Chrome tabs, reacquire a tab, claim a
  tab, navigate, or perform any provider action while implementing/reviewing
  V50.
- V50 design PASS authorizes implementation only, not live execution.
- Preserve the exact 12-path dirty product baseline and keep the index empty
  except for one exact artifact being committed. Never use `git add .`.
- Use exact-path staging/commits for ignored `.superpowers` and `.planning`
  artifacts. Do not stage or rewrite the 12 product paths.
- Concurrent tasks may use other tabs in Chrome Profile
  `Codex-Chrome-Bell-PC2` only when tabs and provider objects are disjoint. The
  continuation owns only the freshly confirmed API Tokens tab and Cloudflare
  token object once a later reviewed live gate begins.
- Prox-01 and VM1205 were confirmed online, but every live checkpoint must be
  revalidated at action time. Do not power, reboot, reconfigure, or broaden VM,
  Proxmox, firewall, credential, DNS, or routing scope.
- No background proxy, listener, owner, routing, credential, or VM process was
  started by the stopped task.
- Never print, commit, log, or retain raw credentials, token values, tab ids,
  account ids, raw listings, or other secret/private provider data.
- Final Create/native Copy/native masked Paste confirmation remains mandatory
  at action time even under standing unattended authority.
- Later exact token-row deletion requires a separate action-time confirmation.
- User pre-approves the safest routine recommendation. Ask for recommendation
  approval only when a proposal is simultaneously super-critical, materially
  different from the reviewed plan, and materially more expensive than the
  initial plan. Mandatory external confirmations and higher-priority safety
  boundaries still apply.
- When this continuation reaches 85% context, repeat this durable rollover,
  create a new continuation, transfer ownership, and leave the old task
  stopped and unarchived.

## Exact dirty baseline

The following 12 concrete status entries belong to the existing product work
and must be preserved byte-for-byte unless later implementation work explicitly
targets them:

- ` M open-sse/services/codexQuotaFetcher.ts`
- ` M src/app/api/v1/models/catalog.ts`
- ` M src/lib/localDb.ts`
- ` M tests/unit/api/models-agent-route-aliases.test.ts`
- ` M tests/unit/services/agent-route.test.ts`
- `?? open-sse/services/agentRoute.ts`
- `?? open-sse/services/agentRouteObjectives.ts`
- `?? src/app/api/v1/agent-routes/events/route.ts`
- `?? src/lib/db/agentRouteRuns.ts`
- `?? src/lib/db/migrations/134_agent_route_runs.sql`
- `?? tests/unit/api/agent-route-events.test.ts`
- `?? tests/unit/db/agent-route-runs.test.ts`

## V49 terminal evidence

- Classification: `1d4481262481c039272ee90e4f97fc7beaae6011`
- Live-result commit: `d1c70a8a0547f23a441e7cf031a53b9198de4b37`
- Declaration audit: 104/104 absent, PASS
- Attachment setup through `openTabs`: completed once
- Listing: `listingValidated=false`, sanitized `offeredCount=-1`
- Claim/navigation/readiness/provider actions: all zero
- Cleanup proof: PASS exactly once
- Realm reset: successful
- Token/provider/VM/DNS/routing change: none

## V50 reviewed design boundary

- Initial design: `658ca83801279f65ca52b183cb01006473565ec4`
- Initial Sol review FAIL: `7245811183e338065e00a6728f7a10d4e8376c18`
  with one IMPORTANT finding, V50-001
- Corrected design: `22ed4ee1c` (resolve to full hash before implementation)
- Independent Sol High design PASS:
  `be427102721e31d86a95d2a0309e46bea3dbb71a`
- PASS counts: Critical 0, HIGH 0, IMPORTANT 0, Minor 0
- `implementation_may_proceed=true`
- `authorizes_live_execution=false`

V50 makes one semantic change: validate the entire bounded offered array and
select exactly one returned record whose safe URL string equals
`https://dash.cloudflare.com/profile/api-tokens`. An absent optional URL and
safe unrelated URLs—including `http:`, internal-scheme, and explicit-port
values—are nonmatches, not failures. Hostile structure, accessors, symbols,
unknown keys, unsafe values, zero exact matches, or duplicate exact matches
fail before claim. After selection, preserve the complete V49/V4 account-home,
token-page, counter, privacy, retention, cleanup, and no-retry semantics.

## Immediate next action

1. Verify HEAD's parent equals the safe-pause handoff parent above, HEAD changes
   exactly the two handoff paths, the index is empty, and the exact 12-path
   dirty baseline remains.
2. Mechanically derive the smallest V50 brief, implementation plan, executable,
   and pure fixture from final V49 plus corrected design `22ed4ee1c`.
3. Add fixtures for exact target among unrelated safe records, URL absent,
   `http:`, internal-scheme, explicit-port, zero exact target, duplicate exact
   target, hostile records, all V49 predecessor globals, exact counters,
   privacy, PASS retention, and one-proof-only cleanup.
4. Run syntax and pure fixtures locally; commit candidates by exact path only.
5. Create a direct-byte review package and obtain independent Sol High
   zero-finding implementation PASS. Reviewers must not perform live actions.
6. Only after PASS, create a later non-self-referential execution
   classification and separate coordinator tuple. Do not initialize CUA yet.

## Last known offline/action-time pins — revalidate later

- V49 pre-send outside projection: `10661` records,
  `C4C85893769D133224AC7404B628C8B0D4B0EBBFF471620B158E652E152B4EBD`
- Evidence worktree clean at
  `80adaa7d5d63d1d2c7bfa63b236c6bee93b3b1d8`
- Runtime SHA:
  `A50E341988B45C547B02DB74787AA64AFFC64F42B5C605308823EEC853179298`
- Documentation SHA:
  `A7C8D53096EA2563CF939D5F7A37DFC2AE0701E891510566930868D04CE0F5DF`
- Public `ai-api-omniroute.mysw.me` A/CNAME absent through 1.1.1.1 and 8.8.8.8
- Windows scoped residue zero
- VM1205: `omniroute` and `bell-cloudflare-proxy` running;
  `team-api-proxy` and `omniroute-team-tunnel` absent; port 20130 absent;
  retained file SHA
  `a31c2010bb47767e25a826cebcd2469cb51d8af5c5ae089e46d2051edd69d9bb`

These are historical pins, not current proof.
