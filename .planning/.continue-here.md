---
context: default
phase: omniroute-live-integration
task: 3
total_tasks: 5
status: paused
last_updated: 2026-08-30T13:49:40+07:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Missing chat-boundary RED contract — Task 3 must not wire `chat.ts` until the exact UUID-header, auth/latch, handler-placement, existing dispatch-binding, buffer-before-exposure, and sanitized-header tests exist and receive independent review.
- [ ] CONSTRAINT: Preserved uncommitted Task 3 work — do not reset, clean, stash, overwrite, or broad-stage the current worktree.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Production dispatch before RED boundary | Task 2 generic controller tests did not lock the security-sensitive chat integration, causing Task 3 to stop before inventing behavior. | blocking | Correct Task 2 in a separate worktree, prove RED, independently review, then resume Task 3. |
| Broad cleanup of a dirty source worktree | The partial Task 3 implementation is valuable and uncommitted. | blocking | Preserve exact files and use exact-path commits only. |
| Treating Windows EPERM cleanup as product failure or permission to relax tests | 227 auth/authz tests passed; two failures were temp-directory cleanup EPERM. | advisory | Re-run in an isolated data directory and retain all assertions. |

<current_state>
Task 3 is paused by direct user instruction while Bell-PC Cloudflare connectivity is repaired. HEAD is `2656a396e748b282f02d5ce07ca468774de7da2f`. The partial controller/DB/event/catalog/quota implementation is uncommitted; `src/sse/handlers/chat.ts` is unchanged.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: official Hermes and DeepSeek client protocols — committed, independently reviewed, handoff `PASS_OFFLINE`.
- Task 2: OmniRoute source discovery and controller RED checkpoint — commit `2656a396e748b282f02d5ce07ca468774de7da2f`, independently approved after fix round 1.
- Task 3: partial implementation — scoped controller suite 28/28 PASS; migration numbering and core typecheck PASS; 227 auth/authz tests PASS with two unrelated Windows cleanup EPERM failures.
</completed_work>

<remaining_work>

- Correct Task 2 with focused chat-boundary RED tests in a separate worktree.
- Independently review the correction.
- Complete Task 3 using existing `handleSingleModelChat` and `parseSSEToOpenAIResponse` seams; rerun all prescribed checks and commit exact files.
- Task 4: package/offline-image review and reversible cutover preparation.
- Task 5: Sol High sole-owner live configuration, thirteen-test gate, and rollback proof.
</remaining_work>

<decisions_made>

- Pause OmniRoute controller implementation and prioritize Bell-PC Cloudflare connectivity per the user's latest instruction.
- No new transport or parser is needed: clone the existing OpenAI body per candidate, reuse `executeChatWithBreaker` → `handleChatCore` / `handleSingleModelChat`, and use `parseSSEToOpenAIResponse` for buffered SSE/tool-call reconstruction.
- The four exact authenticated headers are `x-omniroute-task-id`, `x-omniroute-run-id`, `x-omniroute-turn-id`, and `x-omniroute-idempotency-key`.
</decisions_made>

<blockers>
- Missing Task 2 chat-boundary RED coverage: resolved approach exists, correction not yet implemented.
- Bell physical route: Cloudflare URL returns Access 403 without a service token; live repair requires action-time authority and credentials entered privately.
</blockers>

## Required Reading (in order)

1. `.superpowers/sdd/2026-08-30-omniroute-live-integration/task-3-report.md` — exact partial implementation and verification state.
2. `.superpowers/sdd/2026-08-30-omniroute-live-integration/task2-gap-audit.md` — missing RED contracts.
3. `.superpowers/sdd/2026-08-30-omniroute-live-integration/chat-seam-audit.md` — existing safe request/Response and parser seams.
4. `C:\ChatGPT Projects\SW-Selfhosted-Network\.worktrees\omniroute-routing-prep\docs\superpowers\plans\2026-08-30-omniroute-live-integration.md` — governing approved plan.

## Critical Anti-Patterns (do NOT repeat these)

- Do not implement security-sensitive chat integration from generic controller tests; add and review the missing boundary tests first.
- Do not discard or mingle the current uncommitted Task 3 partial files with the Task 2 correction.
- Do not consume live authority while working in the offline source lane.

## Infrastructure State

- VM1205 OmniRoute: live configuration unchanged by Tasks 2-3.
- Bell-PC Cloudflare endpoint: `https://qwen3827bai-on-bellpc.mysw.me/v1`; unauthenticated `/models` returned Cloudflare Access HTTP 403.
- OmniRoute Bell provider still points to the private LAN endpoint and timed out offsite.
- Physical Bell model discovery/inference: NOT PROVEN.

<context>
The implementation stopped at the correct fail-closed boundary. The source audits show the existing OmniRoute transport and stream parser are sufficient, so resumption should be a small test-contract correction followed by completion of the preserved Task 3 diff.
</context>

<next_action>
Start with: create a separate Task 2 correction worktree from `2656a396`, add the missing focused RED tests, and obtain independent review before touching the preserved Task 3 partial files.
</next_action>
