---
context: default
phase: omniroute-secure-console-transfer
task: 3
total_tasks: 6
status: paused
last_updated: 2026-08-31T16:16:36+07:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint was discovered through an actual failure. Acknowledge each explicitly before proceeding.

- [ ] CONSTRAINT: The terminal bridge is rejected — its outer deadline can abandon an unsettled browser mutation; never execute or revive that path.
- [ ] CONSTRAINT: Browser rejection means uncertain state — after any rejected browser-client promise, issue no later browser or clipboard mutation and do not retry.
- [ ] CONSTRAINT: The direct-v2 and retained-tab-close one-shot authorities are spent — they cannot authorize v3.
- [ ] CONSTRAINT: Clipboard transport is NOT PROVEN — the manual all-tab close resolved only the retained-tab residual.
- [ ] CONSTRAINT: Preserve the unrelated dirty source baseline — no reset, clean, stash, overwrite, or broad stage.
- [ ] CONSTRAINT: Specific security actions remain action-time gated — the earlier unattended request does not authorize a future credential transmission, permission change, or deletion.

**Do not proceed until all boxes are acknowledged.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Timeout around a live browser mutation | The rejected bridge could time out while a browser operation remained capable of completing later. | blocking | Do not execute the bridge; v3 must use strictly serial awaited browser calls with fail-closed rejection handling. |
| Retrying an uncertain one-shot operation | Direct-v2 navigation and the automated retained-tab close each rejected after a prior mutation might have occurred. | blocking | Treat rejection as final uncertainty; no retry and no later browser/clipboard mutation. |
| Converting manual cleanup into transport proof | The user manually closed all Chrome tabs and read-only verification returned zero tabs. | blocking | Record residual resolution only; require a new reviewed v3 and a successful three-call proof for transport. |
| Broad staging in a dirty worktree | Partial agent-routing source work predates this transport lane. | blocking | Stage and commit exact handoff/review paths only. |

<current_state>
Task 3 of the secure-console transfer plan is paused before any v3 artifact is written. HEAD was `d62ae9698bae9f76fee605223416092a83f69e8f` before this handoff commit. The user manually closed all Chrome tabs; a read-only `chrome.tabs.list()` returned `tabCount: 0`, and the stale local retained-tab binding was set to null without a browser mutation. Independent review at `d62ae9698` classified this `PASS — RETAINED-TAB RESIDUAL RESOLVED ONLY`. Clipboard transport remains NOT PROVEN and no execution authority exists for v3.
</current_state>

<completed_work>

- Secure-console transfer design passed independent review at `d22c02505`.
- Terminal bridge path ended `FAIL / NOT AUTHORIZED` at `16e884393`; it was never executed.
- Direct clipboard preflight v2 passed static review at `936a17ebc`, then one approved execution ended `BROWSER_UNCERTAIN`; report `e7c597509`, classification `cf5813b94`.
- One independently approved retained-tab close call ended `CLOSE_UNCERTAIN`; classification `fa6d15aab`.
- User manually closed all tabs; read-only zero-tab verification plus local stale-binding cleanup was recorded at `8431d2cb` and independently classified at `d62ae9698`.
</completed_work>

<remaining_work>

- Write the minimal v3 static brief. Use a one-use Node stdlib HTTP server bound to `127.0.0.1` on an ephemeral port and a random exact path; do not use top-level `data:` navigation.
- Preserve v2's corrected PowerShell baseline/compare/clear logic, strict counters, exact-tab ownership, serial awaits, and `BROWSER_UNCERTAIN` stop rules.
- Define server start/response/close state and make any uncertainty block Call 3. Server cleanup may continue after browser rejection only as local-resource cleanup; no later browser or clipboard mutation.
- Verify direct bytes, encoding, fenced PowerShell parsing, JavaScript syntax, and `git diff --check`; commit the exact brief only.
- Obtain fresh independent Sol High static review. Fix/review until PASS or the contract's limit; do not execute during review.
- After PASS, request fresh exact user approval for one non-secret v3 attempt. Keep credential authority separate.
- Only after successful v3 proof, prepare/review the credential-owner brief and request specific action-time credential authority.
- Continue the standard-team API live tasks through redacted evidence and the final broad Sol High security audit.
</remaining_work>

<decisions_made>

- The replacement v3 should use the platform's Node `http` stdlib; no new dependency or reusable abstraction is needed.
- A rejected browser promise is a terminal uncertainty classification, not permission to inspect, clean up through the browser, or retry.
- The human all-tab close proves only that no Chrome tabs remained at the later read-only observation.
- This task transfers ownership to a fresh same-directory continuation task at the user's request.
</decisions_made>

<blockers>

- Clipboard transport is NOT PROVEN; credential and routing work must not resume yet.
- Fresh v3 execution requires independent PASS plus a new exact one-shot user approval.
- Credential creation/transmission, permission changes, and deletion each require their own later action-time authorization.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable current state and exact next action.
2. `.superpowers/sdd/2026-08-31-omniroute-secure-console-transfer/progress.md` — chronological task ledger.
3. `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-direct-clipboard-preflight-v2-live-brief.md` — last approved direct contract; reuse only its proven static structure.
4. `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-direct-clipboard-preflight-v2-live-report.md` — exact fail-closed execution outcome.
5. `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-direct-preflight-retained-tab-disposition-report.md` — automated close uncertainty and manual disposition evidence.
6. `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-direct-preflight-retained-tab-disposition-sol-review.md` — independent residual-only PASS.
7. `docs/superpowers/plans/2026-08-31-omniroute-secure-console-transfer.md` — governing transfer plan.
8. `docs/superpowers/plans/2026-08-30-omniroute-standard-team-api.md` — downstream live-routing plan.

## Critical Anti-Patterns (do NOT repeat these)

- Do not execute the terminal bridge or reuse its rejected timeout model.
- Do not retry direct-v2, its `data:` navigation, or the retained-tab close.
- Do not infer clipboard success from a created tab, a manual close, or missing tabs.
- Do not put a challenge or secret in reports, tool output, Git, shell history, or task messages.
- Do not combine non-secret preflight approval with credential or routing authority.

## Infrastructure State

- Chrome: zero tabs at the last read-only observation; stale retained-tab binding null.
- Browser/clipboard: no active operation or server; clipboard contents were not re-read after v2 failed.
- VM1205, Cloudflare, OmniRoute, Hermes, DeepSeek, providers, credentials, permissions, and routes: unchanged by this lane.
- Git: branch `codex/omniroute-agent-routing`; unrelated dirty source baseline preserved.

## Pre-Execution Critique Required

- Design artifact: `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-loopback-clipboard-preflight-v3-live-brief.md` (not yet created).
- Critique focus: loopback-only server ownership/lifecycle, random exact path, no-store response, strict serial browser calls, rejected-promise uncertainty, exact tab/server cleanup accounting, Call-3 eligibility, bounded redacted output, and no authority expansion.
- Gate: Do not execute v3 until the exact committed bytes receive independent Sol High PASS and the user then gives fresh exact one-shot approval.

<context>
The shortest safe replacement is a one-use loopback page served inside the same Node REPL cell. It avoids the failed `data:` navigation while keeping the test non-secret and bounded. Static design/review is authorized; browser, clipboard, credential, permission, deletion, and routing mutations are not.
</context>

<next_action>
Start with: verify this source task is archived, acknowledge every blocking constraint, read the required artifacts, revalidate Chrome tab count read-only, then write and independently review the v3 static brief only.
</next_action>
