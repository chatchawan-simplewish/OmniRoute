# OmniRoute V27 settled-home Domains-route reacquisition — execution classification

## Classification

**STATIC PASS — eligible for one live evaluation only after this classification
is committed in a one-path commit, its separate post-commit coordinator tuple
is recorded, and every fresh action-time pin passes.**

This classification authorizes no action by itself and contains no claim about
its own commit, parent, bytes, hash, blob, HEAD, projection, or runtime state.

## Reviewed evidence

- V25 consumed-PASS-clean report:
  `01a5977398cf5d653e1bfe81a76cd6cb32439454`.
- V26 consumed-failed-clean incident:
  `04bc5e467caa057ab44ea49935a6b66c8e87a1b2`.
- Current V27 brief:
  `2c84d6a23e321b1ffc9851358f90cc46b6eef899`.
- Independent Sol High PASS review:
  `cbc51d59c791510ff6a22661124303d9b0bbd774`.

The reviewed V27 brief is `22748` committed bytes at SHA-256
`85DEB41D561B79D63939A38FD2A9E0509F4AF52108870C38D4FC9EE344C2F423`
and blob `08fba2f841a9fc4a5bae0909c5ca487e93f809eb`. Its normalized fenced executable
is `18432` UTF-8 bytes at SHA-256
`8C929CC532186B34B3B3FAF67E370ACCFEC5EE825E652BED90F6F70E355440B2`.
Syntax passed. Static call sites are declarations `7`, new `1`, goto `2`, URL
`4`, wait `3`, click `1`, close `1`, and write `1`.

The PASS review is `8049` bytes at SHA-256
`33F319811D690038FD0FE6705EE9483A48973AF6E9BFF0C63E8EF6A28BE43D34`
and blob `f708b46df0cf340cc2fc91f640d3134e19a94423`. It reports zero unresolved
Critical, HIGH, IMPORTANT, or Minor findings.

## Exact eligible boundary

V23 readiness is consumed PASS and V23 prestart is consumed failed-clean with
no retained tab. V24 was never evaluated and its declarations remain absent.
V25 is consumed PASS-clean with no retained tab. V26 is consumed failed-clean
after its first URL read, before any snapshot or click, with no retained tab.
V27 has never been evaluated and all seven V27 declarations must remain absent
until the sole reviewed call.

The sole call may create one exact owned tab; make one fixed dashboard-home
navigation; perform a host-only first URL check; condition-wait for the first
visible anchor; perform a distinct settled-home URL read; bind the original
account segment locally; validate the complete V25-derived fixed-key signature;
click exactly one visible uniquely counted Domains action; and require the
Domains route, zone href, and final zone route all to retain that account. Exact
PASS retains only the validated zone tab for a separately reviewed read gate.

No URL, href, account/zone ID, DOM, HTML, screenshot, provider response,
credential, token, secret, or clipboard value may be emitted. No DNS,
rate-rule, Tunnel, Access, API-token, permission, Create, edit, delete, fill,
press, submit, VM, routing, listener, process, reconnect, or tab-discovery
action is allowed. The sole click is read-only navigation.

Any non-PASS permanently spends V27. There is no retry, fallback,
continuation, reinterpretation, manual integration, verdict relaxation, or
alternate browser path. Failure closes only the exact owned tab or retains
that exact handle as residue evidence.

## Action-time requirements

Before the sole call, revalidate exact committed brief/executable/review and
classification bytes, direct-parent/one-path shape, separate post-commit tuple,
source projection, empty index, exact 12-path baseline, browser runtime hashes,
clean evidence worktree, zero residue, VM1205/DNS safe state, exact
V23/V25/V26 persistent state, V24 declaration absence, and all V27 declarations
absent.

The mandatory combined final Create/native Copy/native masked Paste
confirmation and later separate exact-row deletion confirmation remain
unreached and mandatory.

`authorizes_live_execution=false`
