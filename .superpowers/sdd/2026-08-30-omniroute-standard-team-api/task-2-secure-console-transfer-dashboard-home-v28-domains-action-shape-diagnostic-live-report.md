# V28 Domains-action shape diagnostic live report

## Verdict

`PASS_CLEAN` — V28 was consumed exactly once and returned
`EXACT_V28_DOMAINS_ACTION_SHAPE_DIAGNOSTIC_PASS`.

No click, fill, press, provider write, secret read, secret write, token action,
or provider-persistent change occurred. The exact created tab closed and no
retained handle remains. V28 is spent and must never be retried, continued, or
reinterpreted.

## Reviewed execution tuple

- Fix 1 brief commit: `cf062d27fac8d7912cfea32ea32414302f482172`.
- Fix 1 independent Sol High PASS review:
  `36b76e349825a349fdd4df78d5c4170f55c92de9`.
- Non-self-referential classification:
  `4a584121b794dc6262a00833eedb7dd86daa2133`.
- Executable: `17125` normalized LF bytes, SHA-256
  `AD92BA14844D1F015720EC68DE0F794A2F85F1CD09D775C940616B2101E4DFAD`.
- Action-time coordinator tuple: HEAD
  `4a584121b794dc6262a00833eedb7dd86daa2133`; chain/exclusions/records
  `101/103/10661`; source projection
  `C4C85893769D133224AC7404B628C8B0D4B0EBBFF471620B158E652E152B4EBD`;
  index `0`; exact dirty baseline `12`.
- Pinned Chrome skill/client/API byte counts and hashes matched. Evidence
  worktree HEAD was `80adaa7d5d63d1d2c7bfa63b236c6bee93b3b1d8` and clean. Temporary
  residue and owner/R5 process counts were zero.
- VM1205 retained the safe checkpoint: `omniroute` and
  `bell-cloudflare-proxy` running; `team-api-proxy` and
  `omniroute-team-tunnel` absent; network members `2`; retained Caddy SHA-256
  `a31c2010bb47767e25a826cebcd2469cb51d8af5c5ae089e46d2051edd69d9bb`;
  listener `20130` absent.
- Public A/CNAME counts for `ai-api-omniroute.mysw.me` were `0/0` at both
  `1.1.1.1` and `8.8.8.8`.
- Persistent predecessor state was exact and all V28 declarations were absent.

## Pre-consumption wrapper event

The first local orchestration wrapper stopped before calling the Node tool
because its V8 isolate did not provide `atob`. A no-browser persistent-state
check immediately proved all three V28 declarations remained absent. No V28
code or browser action had occurred. The wrapper then passed the same
commit-extracted executable directly as JSON after revalidating HEAD, empty
index, exact 12-path dirty baseline, byte count, and SHA-256. Only that Node
call consumed V28.

## Fixed structural evidence

- Settled URL: exact Cloudflare host and account-home path.
- Unique Domains control: count `1`, visible count `1`.
- Element shape: native `button`; not an anchor; no link/button ARIA role.
- Navigation data: no `href`, no same-host/account-domains href, no
  `data-href`, no `target`, and no `download`.
- Activation shape: no element-level native `click()` method; not disabled,
  aria-disabled, or inert; positive rectangle; within viewport; center hit is
  self or descendant; pointer events are enabled; keyboard tab stop present.
- Controlled target: `aria-controls` present and its target exists.
- Anchors: all `48`, visible `46`; exact/containing zone-text anchors `0/0`;
  exact/nested zone hrefs `0/0`; account root/home/domains hrefs `0/0/0`.
- Other bounded counts: websites `0`, overview `1`, account `0`, main `0`,
  navigation `1`, busy `0`.

## Completeness and cleanup

- Declaration shape, predecessor exactness, controller ownership, created
  handle capture, tab shape, snapshot validation, and snapshot completeness:
  all true.
- Attempts/fulfilments: new `1/1`, navigation `1/1`, initial URL `1/1`, wait
  `1/1`, settled URL `1/1`, snapshot `1/1`, close `1/1`, write attempted `1`.
- Cleanup: `EXACT_TAB_CLOSED`; residue converged true; retained exact handle
  false; state `V28_DIAGNOSTIC_PASS_CLEAN`; error class `NONE`.

## Next boundary

Any successor must be a fresh reviewed contract. It may use only the proven
unique, visible, enabled, hit-testable Domains button and its existing
`aria-controls` target. V28 itself provides no authority to activate the
control, navigate, inspect provider data, create a token, or perform any
provider mutation.
