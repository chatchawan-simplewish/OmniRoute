# OmniRoute V28 Domains-action shape diagnostic — independent Sol High review

Date: `2026-09-02` (`Asia/Bangkok`)

`authorizes_live_execution=false`

## Scope and authority

This review covers only commit
`372948212ff8678754b31575c4ee95810d736f30` and exact path
`.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-secure-console-transfer-dashboard-home-v28-domains-action-shape-diagnostic-brief.md`.
Its direct parent is consumed V27 failed-clean incident commit
`30bdcaa68ed5349e93905937683a6e7d286a9104`.

I independently reviewed direct committed bytes, syntax, the exact V27 and
three-attempt predecessor boundary, diagnostic-only scope, fixed-key
cross-realm projection, account-local href booleans, output non-leakage, zero
activation, exact-handle cleanup, call cardinality, no-mutation/no-retry
semantics, secret exclusions, and both mandatory manual confirmations.

I performed no Chrome, browser binding, provider, clipboard, credential,
network, DNS, routing, process, Prox-01, or VM action and did not evaluate the
V28 cell. The only workspace write is this assigned review artifact.

## Final verdict

**FAIL** — one unresolved IMPORTANT finding. The executable does not require
the six V23/V26/V27 detach-consumed flags to remain false, so its predecessor
predicate is weaker than the exact failed-clean three-attempt state that V28 is
authorized to diagnose.

`authorizes_live_execution=false`

## Direct-byte, syntax, and Git evidence

- Reviewed commit: `372948212ff8678754b31575c4ee95810d736f30`.
- Direct parent: `30bdcaa68ed5349e93905937683a6e7d286a9104`.
- The reviewed commit adds exactly the assigned V28 brief path.
- Brief: `19445` bytes, SHA-256
  `057AF2A719AD08B4427A016CDEC1288BC26C67830F683E164B1B1BB07EA7B563`,
  Git blob `6da9eb5d0080034f6787eeace9c6f75175de6953`.
- Normalized executable: `16225` UTF-8 bytes, SHA-256
  `E6EFCCDDD396EE47D4989D9533E7BA72CBBA24A0491AD6B99142BD5DEC3F42BF`.
- Encoding is UTF-8 without BOM and LF-only. Non-evaluating Acorn module parse
  with top-level await: **PASS**.
- Three V28 persistent declarations are present exactly once.
- `git diff-tree --check` reports no whitespace error.
- Before review creation, the index held zero paths and the exact inherited
  twelve-path dirty baseline was present and unstaged.

## V28-001 — IMPORTANT: detach state is absent from the predecessor

The V27 incident proves the third guarded route attempt failed clean after the
settled-home, complete signature, unique Domains count, and visibility checks;
the sole click attempted `1` and fulfilled `0`, no later step began, exact close
completed `1 / 1`, residue converged, and no handle remained. It also records
that both V27 detach flags were false. V27 itself required both V23 and both V26
detach flags false before its sole call.

V28 correctly requires the consumed failed-clean route/read states and null
retained handles for V23, V26, and V27. It does not reference any of these six
persistent fields:

- `secureConsoleOwnedTaskTabV23PreCreateDetachConsumed`;
- `secureConsoleOwnedTaskTabV23PostNativeDetachConsumed`;
- `secureConsoleDashboardV26PreCreateDetachConsumed`;
- `secureConsoleDashboardV26PostNativeDetachConsumed`;
- `secureConsoleDashboardV27PreCreateDetachConsumed`; and
- `secureConsoleDashboardV27PostNativeDetachConsumed`.

Consequently, V28 can pass its predecessor after one of those fields has
drifted to true. That state is not the exact incident chain and can indicate an
intervening native-detach or handoff boundary that V28 was not reviewed to
follow. The diagnostic gate would then be spent and its result attributed to
the three-attempt architecture despite a changed authority/ownership history.

The V28 action is read-only and always attempts cleanup, so this gap does not by
itself create a provider mutation or expose a secret. It is nevertheless an
IMPORTANT fail-closed predecessor violation in a one-shot security contract.

### Required correction

Add all six fields to declaration/predecessor validation and require each exact
value to be `false`, matching the V23/V26/V27 incident chain. Re-pin the revised
direct committed bytes and obtain a fresh independent review before any call.

## Closed checks outside V28-001

### Diagnostic-only architecture and zero activation

V28 follows the V27 incident's architectural stop: it makes no fourth
activation guess. The fixed dashboard-home navigation is followed by exactly
one bounded first-visible-anchor wait and a second settled-home URL check. It
then performs one synchronous body snapshot and always proceeds to exact-tab
cleanup. There is no click, native click invocation, fill, press, submit,
keyboard event, listener, Create, edit, delete, provider mutation, or post-home
navigation call site.

Static call sites reproduce exactly: declarations `3`; `tabs.new` `1`; `goto`
`1`; URL reads `2`; wait `1`; evaluator `1`; exact-handle close `1`; terminal
write `1`; and click/fill/press `0 / 0 / 0`. Selected/list/get discovery,
clipboard, screenshot, fetch, reconnect, retry, fallback, and alternate-tab
sites are zero.

### Fixed-key cross-realm projection and completeness

The snapshot has exact equality over 26 boolean keys and 17 integer keys. The
pinned cross-realm plain-record rule accepts only a direct null prototype or a
prototype whose parent is null. Every boolean has an explicit boolean type; all
integers must be safe, nonnegative, and no greater than `1000000`. Only those
fixed keys are copied into a new trusted controller-local object.

The structural booleans cover tag anchor/button, role link/button, href
presence and same-host/same-original-account Domains shape, native-click
presence, disabled/ARIA-disabled/inert state, rectangle/viewport/center hit,
pointer events, target/download/ARIA-controls/data-href/tab-stop shape. Raw tag,
role, href, ID, attribute, URL, path, account, text, DOM, and style values remain
page/controller local.

The inherited page signature includes exact host/path booleans, complete
anchor/route/action/structure counts, exact unique Domains count, exactly one
visible Domains action, and zero busy markers. The settled account segment is
passed inward only for a fixed same-account equality boolean and is never
returned.

### Fixed output and no leakage

Terminal output includes only fixed result/state/cleanup strings, sanitized
error class, booleans, bounded integer counts, counters, consumed state, and
the validated fixed-key snapshot. There is no raw untrusted key or value and no
tag name, role string, href, URL, path, account/zone identifier, text, DOM,
HTML, attribute value, screenshot, provider response, credential, token,
secret, or clipboard value.

### Exact-handle cleanup and no retry

The sole created handle is chained into local and durable V28 bindings before
every later await. Every result, including diagnostic PASS, must close that
exact handle. The durable binding clears only after close fulfillment; close
rejection retains the exact handle, forces non-PASS, and reports unconverged
residue. Rejected/uncertain creation or malformed/no-handle outcomes cannot be
reported clean; only genuine precreation failure is clean.

There is no tab discovery, reacquisition, continuation, reinterpretation,
manual integration, retry, fallback, alternate activation, or verdict
relaxation. Both the final Create/native Copy/native masked Paste confirmation
and the later separate exact-row deletion confirmation remain explicit,
unreached, and mandatory.

## Finding counts and limits

- Critical: `0`.
- HIGH: `0`.
- IMPORTANT: `1` unresolved (`V28-001`).
- Minor: `0`.

This static review does not authorize live execution. No classification or
post-commit tuple may override `V28-001`.

## Final verdict

**FAIL**

Unresolved findings: Critical `0`, HIGH `0`, IMPORTANT `1`, Minor `0`.

`authorizes_live_execution=false`
