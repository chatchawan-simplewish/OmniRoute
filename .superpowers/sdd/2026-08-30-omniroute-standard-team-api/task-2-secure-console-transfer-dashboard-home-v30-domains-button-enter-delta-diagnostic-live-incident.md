# OmniRoute V30 Domains-button Enter delta diagnostic live incident

## Terminal classification

`CONSUMED_FAILED_CLEAN`

V30 was consumed exactly once. The reviewed non-form Domains button received
one fulfilled Enter press, but the first post-press page snapshot rejected
before producing a value. The exact created tab then closed once. V30 is spent
and must never be retried, continued, reinterpreted, or used as authority for a
successor.

## Pinned execution identity

- V30 brief: `69d6f486eb7a635cc060b2b0964465c16df319bb`;
  `28812` bytes; SHA-256
  `9D36AAEAB12EE60C3E9FE527E6D8D9CF436C4FDB4DFF437B57BE71DC7ED0B8E4`;
  blob `e36fd3b20b5c70d122fced40b2e42b8f15ad9094`.
- Executable: `24361` normalized LF bytes; SHA-256
  `C7211FA5E11E1D9CDBC3E476B2325DDD2485D76586FD03FD55DF6AB2DB565A3F`.
- Independent Sol High PASS review:
  `6e098d4afb036c4b37fa1a9ea51570d42301b2c7`.
- Non-self-referential classification:
  `9aa5c9ff03283aa931394ae1fe60d3b1b83e6a24`.
- Action-time coordinator tuple: chain/exclusions/records `110/112/10661`;
  projection
  `C4C85893769D133224AC7404B628C8B0D4B0EBBFF471620B158E652E152B4EBD`;
  index `0`; exact dirty baseline `12`.
- Runtime, evidence worktree, temporary/process residue, VM1205, public DNS,
  persistent-controller, exact-predecessor, and absent-V30-declaration pins all
  passed immediately before consumption.

Two local orchestration prestarts stopped before any V30 Node call: the first
used an unavailable orchestration-only `TextEncoder`; the second trimmed the
leading porcelain status marker. After each stop, a fixed state read proved all
V30 declarations absent. Neither prestart received the executable in the
persistent Node session and neither consumed V30.

## Fixed pre-press evidence

- Exact Cloudflare host and account-home path: true/true.
- Unique visible native Domains button: true/true/true; counts `1/1`.
- Href/native click absent: true/true.
- Enabled/non-inert/non-form: true/true/true.
- Positive in-viewport hit-tested rectangle with pointer events: all true.
- `aria-controls` present, target present/visible, and `aria-expanded=true`
  false: true/true/true/false.
- Tab stop present and busy count zero: true/`0`.
- Controlled anchors visible: `3/3`; controlled account-Domains and zone href
  counts: `0/0`.

## Fixed call and cleanup evidence

- New tab `1/1`; navigation `1/1`; initial/settled URL reads `1/1` each;
  initial wait `1/1`; pre-snapshot `1/1`.
- Locator count and target-safety evaluation `1/1` each; target safe true.
- Enter press `1/1`.
- Post-snapshot `1/0`; validation false; completeness false; snapshot null.
- Close `1/1`; terminal write attempted `1`.
- Cleanup `EXACT_TAB_CLOSED`; residue converged true; retained exact handle
  false; state `V30_DIAGNOSTIC_FAILED_CLEAN`; consumed true; sanitized error
  class `Error`.

## Replacement boundary

The evidence proves the one Enter press fulfilled, then the page-local async
post-snapshot rejected before returning fixed evidence. It does not prove
whether the controlled menu changed, a route transition began, or the final
post-press page state. No provider-persistent change, secret operation, Create,
copy, paste, edit, delete, DNS, routing, listener, VM, credential, or permission
action occurred.

Any successor must be a fresh separately committed, independently Sol High
reviewed, non-self-referentially classified, action-time-pinned one-shot
contract. It may replace the page-local async polling evaluator with bounded
outer-realm waits and synchronous fixed-shape samples that tolerate a page
transition without repeating the Enter press. This incident authorizes no
successor call.
