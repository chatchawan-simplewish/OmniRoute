# Task 2 secure-console transfer — retained-Chrome Option A recovery

`authorizes_live_execution=false`

Status: fresh static one-shot recovery candidate for independent Sol High
review. This is not a retry, reconstruction, restoration, continuation, or
reuse of the spent fresh-connection amendment. It performs and authorizes no
JavaScript, Chrome, tab, clipboard, credential, process, Cloudflare, VM,
proxy, routing, permission, evidence, or deletion action.

## Exact pins and prior disposition

- Spent fresh-Chrome amendment commit
  `2decbc5e265b4b75fb7a4ea00200087da3400990`: `16251` bytes, SHA-256
  `37F5C79AF2A6C443DB86742FE814CE1EC46A9DCCFE046B11CFF263946008B0FC`.
- Its independent static PASS review commit
  `fbb52f47259c216e19443bd3e783de23ff20b4d6`: review file `10575` bytes,
  SHA-256
  `5B6AE84E23AFF06DC19D340D276470093F7C3C37FA2B02E3C6863AA068E3EF0C`.
- Spent Call-1 live report commit
  `20e842690fa52f5a72d458c38d4b2e399d848293`: report `2861` bytes,
  SHA-256
  `64C483F0B0F0C8A4B9C8A24E04B2A5344060E898EE596A2F7B1948232C414876`.
- Independent classification commit
  `a6ba3fcbf9033f4d313ae883586512671409d1d4`: classification `9052` bytes,
  SHA-256
  `20FD07D157B2A8332D2D3FE293FC79BA88EFF8C08A274888F3ABD3F3C31B07CA`.
- The closed approved live brief remains commit
  `7af3ab75ec87d81d75811ff8f2e9b4fa9d0c3e3b`, `62054` bytes, SHA-256
  `995CA4D59E564EAF53417B9F577808F9C307C5CDEE93FB8E91DC73A5211F52CC`;
  its independent PASS is
  `ce47c2eacb5a5cbf055c3fc814137e46c1855e40`.
- Future redacted report:
  `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-secure-console-transfer-retained-chrome-recovery-report.md`.
- Future independent classification:
  `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-secure-console-transfer-retained-chrome-recovery-classification.md`.

The spent Call 1 delivered complete selected-Chrome documentation but omitted
its required redacted terminal because it ended with a bare object expression.
Exact original Call-1 success remains `NOT PROVEN` and is never reconstructed.
Call 2 and `tabs.list()` remained zero. The credential-consuming gate remains
unconsumed but closed under the spent amendment.

## Same-session and manual preconditions

This Option A recovery exists only while the exact same persistent JavaScript
session used for spent Call 1 remains current and untouched. The possible old
names are evidence candidates, not authority. No new session, reset, import,
bootstrap, documentation call, browser selection, connection, reconnect,
reacquisition, fallback, or alias is permitted.

Only the sole Sol High owner may later consume the one cell, after this exact
brief is committed, independently reviewed `PASS`, and action-time bytes,
commits, session identity, Git index `0`, and unrelated baseline `12` are
pinned. Immediately before consumption, the user must manually close the known
blank Chrome tab and visually confirm that no tab was opened by automation and
no other visible tab remains. No agent, Chrome-control API, Computer Use,
keyboard automation, tab handle, or cleanup code may perform that close. If
manual closure disconnects the retained object, the one cell must fail closed;
there is no reconnection or recovery.

## Exact one-cell recovery

The sole owner invokes exactly one JavaScript-tool cell below in the same
persistent session, with one `30000 ms` tool-control deadline. The outer tool
wrapper must forward the complete tool result exactly once, without slicing,
summarizing, reconstructing, or adding a verdict. Missing, duplicated,
truncated, malformed, rejected, timed-out, or tool-uncertain output spends this
recovery and stops all later Task 2 action.

The cell first uses `typeof` guards for every old identifier. It then validates
the retained object and the exact local Call-1 state as new recovery evidence.
Only that exact new predicate can authorize one count-only `tabs.list()`.
There is exactly one explicit `nodeRepl.write` and no bare terminal-object
expression.

```javascript
const secureConsoleRetainedRecoveryV1DeclarationsPresent =
  typeof secureConsoleAgentV1 !== "undefined" &&
  typeof secureConsoleChromeV1 !== "undefined" &&
  typeof secureConsoleChromeV1ConnectAttempted !== "undefined" &&
  typeof secureConsoleChromeV1ConnectFulfilled !== "undefined" &&
  typeof secureConsoleChromeV1DocumentationAttempted !== "undefined" &&
  typeof secureConsoleChromeV1DocumentationFulfilled !== "undefined" &&
  typeof secureConsoleChromeV1ConnectedShape !== "undefined" &&
  typeof secureConsoleChromeV1ConnectErrorClass !== "undefined" &&
  typeof secureConsoleChromeV1ConnectExact !== "undefined";
let secureConsoleRetainedRecoveryV1AgentShapeValid = false;
let secureConsoleRetainedRecoveryV1ChromeShapeValid = false;
let secureConsoleRetainedRecoveryV1ConnectCountersExact = false;
let secureConsoleRetainedRecoveryV1DocumentationCountersExact = false;
let secureConsoleRetainedRecoveryV1PriorShapeExact = false;
let secureConsoleRetainedRecoveryV1PriorErrorExact = false;
let secureConsoleRetainedRecoveryV1PriorExactBooleanTrue = false;
let secureConsoleRetainedRecoveryV1StateExact = false;
let secureConsoleRetainedRecoveryV1TabListAttempted = 0;
let secureConsoleRetainedRecoveryV1TabListFulfilled = 0;
let secureConsoleRetainedRecoveryV1TabListShapeValid = false;
let secureConsoleRetainedRecoveryV1TabCount = -1;
let secureConsoleRetainedRecoveryV1TabInfos = null;
let secureConsoleRetainedRecoveryV1ErrorClass = "NONE";
try {
  if (!secureConsoleRetainedRecoveryV1DeclarationsPresent) {
    secureConsoleRetainedRecoveryV1ErrorClass = "RETAINED_DECLARATION_MISSING";
  } else {
    secureConsoleRetainedRecoveryV1AgentShapeValid =
      secureConsoleAgentV1 !== null && typeof secureConsoleAgentV1 === "object";
    secureConsoleRetainedRecoveryV1ChromeShapeValid =
      secureConsoleChromeV1 !== null &&
      typeof secureConsoleChromeV1 === "object" &&
      typeof secureConsoleChromeV1.documentation === "function" &&
      secureConsoleChromeV1.tabs !== null &&
      typeof secureConsoleChromeV1.tabs === "object" &&
      typeof secureConsoleChromeV1.tabs.list === "function";
    secureConsoleRetainedRecoveryV1ConnectCountersExact =
      secureConsoleChromeV1ConnectAttempted === 1 &&
      secureConsoleChromeV1ConnectFulfilled === 1;
    secureConsoleRetainedRecoveryV1DocumentationCountersExact =
      secureConsoleChromeV1DocumentationAttempted === 1 &&
      secureConsoleChromeV1DocumentationFulfilled === 1;
    secureConsoleRetainedRecoveryV1PriorShapeExact =
      secureConsoleChromeV1ConnectedShape === true;
    secureConsoleRetainedRecoveryV1PriorErrorExact =
      secureConsoleChromeV1ConnectErrorClass === "NONE";
    secureConsoleRetainedRecoveryV1PriorExactBooleanTrue =
      secureConsoleChromeV1ConnectExact === true;
    secureConsoleRetainedRecoveryV1StateExact =
      secureConsoleRetainedRecoveryV1AgentShapeValid === true &&
      secureConsoleRetainedRecoveryV1ChromeShapeValid === true &&
      secureConsoleRetainedRecoveryV1ConnectCountersExact === true &&
      secureConsoleRetainedRecoveryV1DocumentationCountersExact === true &&
      secureConsoleRetainedRecoveryV1PriorShapeExact === true &&
      secureConsoleRetainedRecoveryV1PriorErrorExact === true &&
      secureConsoleRetainedRecoveryV1PriorExactBooleanTrue === true;
    if (!secureConsoleRetainedRecoveryV1StateExact) {
      secureConsoleRetainedRecoveryV1ErrorClass = "RETAINED_STATE_NOT_EXACT";
    } else {
      secureConsoleRetainedRecoveryV1TabListAttempted++;
      secureConsoleRetainedRecoveryV1TabInfos = await secureConsoleChromeV1.tabs.list();
      secureConsoleRetainedRecoveryV1TabListFulfilled++;
      secureConsoleRetainedRecoveryV1TabListShapeValid =
        Array.isArray(secureConsoleRetainedRecoveryV1TabInfos);
      if (!secureConsoleRetainedRecoveryV1TabListShapeValid) {
        secureConsoleRetainedRecoveryV1ErrorClass = "TAB_LIST_SHAPE_INVALID";
      } else {
        secureConsoleRetainedRecoveryV1TabCount =
          secureConsoleRetainedRecoveryV1TabInfos.length;
        if (!Number.isInteger(secureConsoleRetainedRecoveryV1TabCount) ||
            secureConsoleRetainedRecoveryV1TabCount !== 0) {
          secureConsoleRetainedRecoveryV1ErrorClass = "TAB_COUNT_NOT_ZERO";
        }
      }
    }
  }
} catch {
  secureConsoleRetainedRecoveryV1ErrorClass = "RETAINED_RECOVERY_FAILED_OR_UNCERTAIN";
} finally {
  secureConsoleRetainedRecoveryV1TabInfos = null;
}
const secureConsoleRetainedRecoveryV1Exact =
  secureConsoleRetainedRecoveryV1DeclarationsPresent === true &&
  secureConsoleRetainedRecoveryV1StateExact === true &&
  secureConsoleRetainedRecoveryV1TabListAttempted === 1 &&
  secureConsoleRetainedRecoveryV1TabListFulfilled === 1 &&
  secureConsoleRetainedRecoveryV1TabListShapeValid === true &&
  Number.isInteger(secureConsoleRetainedRecoveryV1TabCount) &&
  secureConsoleRetainedRecoveryV1TabCount === 0 &&
  secureConsoleRetainedRecoveryV1ErrorClass === "NONE";
nodeRepl.write({
  result: secureConsoleRetainedRecoveryV1Exact ?
    "EXACT_RETAINED_CHROME_RECOVERY_ZERO_TABS" :
    "RETAINED_CHROME_RECOVERY_FAILED_STOP",
  declarationsPresent: secureConsoleRetainedRecoveryV1DeclarationsPresent,
  agentShapeValid: secureConsoleRetainedRecoveryV1AgentShapeValid,
  chromeShapeValid: secureConsoleRetainedRecoveryV1ChromeShapeValid,
  connectCountersExact: secureConsoleRetainedRecoveryV1ConnectCountersExact,
  documentationCountersExact: secureConsoleRetainedRecoveryV1DocumentationCountersExact,
  priorShapeExact: secureConsoleRetainedRecoveryV1PriorShapeExact,
  priorErrorExact: secureConsoleRetainedRecoveryV1PriorErrorExact,
  priorExactBooleanTrue: secureConsoleRetainedRecoveryV1PriorExactBooleanTrue,
  recoveryStateExact: secureConsoleRetainedRecoveryV1StateExact,
  tabListAttempted: secureConsoleRetainedRecoveryV1TabListAttempted,
  tabListFulfilled: secureConsoleRetainedRecoveryV1TabListFulfilled,
  tabListShapeValid: secureConsoleRetainedRecoveryV1TabListShapeValid,
  tabCount: secureConsoleRetainedRecoveryV1TabCount,
  errorClass: secureConsoleRetainedRecoveryV1ErrorClass
});
```

Exact PASS requires the one forwarded object and no other cell output:

- `result=EXACT_RETAINED_CHROME_RECOVERY_ZERO_TABS`;
- every declaration/state/shape/error/counter boolean `true`;
- list attempted/fulfilled `1 / 1`, list shape `true`, integer count `0`; and
- `errorClass=NONE`.

The old local state is validated as new recovery evidence only. It does not
prove that the spent Call 1 emitted its missing terminal and must never be
reported as retroactive Call-1 success. The `-1` count is the fixed unknown
sentinel. No browser, agent, tab, or list object; array; exception;
documentation; tab handle; ID; title; URL; favicon; metadata; DOM; content;
screenshot; or element enters output.

## Exact prohibitions and failure disposition

The executable cell contains zero import, bootstrap, setup, documentation,
browser-selection, `browsers.get`, `getDefault`, `getForUrl`, extension/Edge/
IAB selection, browser/global listing, reset, reconnect, reacquisition, alias,
serialization, global scan, exception-text output, tab iteration, element
access, `tabs.get`, `tabs.new`, close, claim, navigation, Playwright,
evaluation, keyboard, clipboard, screenshot, process, credential, or routing
operation. `globalThis`, `Object.keys`, and property-name enumeration are
forbidden. The sole browser method call is the one conditional `tabs.list()`.

Any missing declaration, thrown getter, invalid retained shape, prior counter
mismatch, prior shape/error/exact mismatch, list rejection/malformed result,
nonzero/unknown count, output mismatch, tool uncertainty, or interrupted call
spends this recovery. It performs no tab mutation and stops before clipboard,
precondition revalidation, proxy start, script preparation, owner launch,
Cloudflare read/mutation, credential creation, or routing action. There is no
second cell, retry, repair, fallback, manual verdict, output reconstruction,
or alternate cleanup. A nonzero count requires a new reviewed cleanup contract.

## Sole binding substitution after exact PASS

Only exact recovery PASS makes the already retained
`secureConsoleChromeV1` object eligible to substitute the Chrome binding clause
at lines 158–159 of the previously approved live brief commit
`7af3ab75ec87d81d75811ff8f2e9b4fa9d0c3e3b`:

> `secureConsoleChromeV1` is the exact retained, non-null Chrome object from the
> same persistent JavaScript session; this new recovery cell validated its
> retained local state and its sole count-only `tabs.list()` returned integer
> zero. No new connection, reacquisition, or tab metadata output occurred.

This substitution never revives or reuses the spent amendment. The approved
brief file is not edited. Every other approved byte-level target, permission,
counter, owner/R5 script, timeout, confirmation, revocation, cleanup,
redaction, failure, and exclusion remains intact.

## Preserved full revalidation and live boundaries

Recovery PASS is necessary but insufficient for later consumption. The sole
owner must still freshly revalidate the complete approved serial precondition
set before any consuming action:

- exact recovery/PASS, approved brief/PASS, R5/proxy/Caddy/pwsh bytes/hashes,
  evidence worktree commit/cleanliness, Git index, and baseline;
- exact Windows CIM/registry identity and both machine clipboard-policy DWORDs
  at `0 / 0`;
- exactly one current Windows clipboard clear/read-empty proof, no retry and no
  history-erasure claim;
- the same exact retained `secureConsoleChromeV1` identity and this already
  consumed recovery count `0`, with no second list or connection;
- VM1205 `omniroute` and `bell-cloudflare-proxy` running, target proxy/Tunnel
  absent, network members `2`, listener `20130` absent, and pinned Caddy hash;
- Cloudflare safe-count proof for the unrelated rule, zero target rule,
  DNS/Tunnel/connector/overlapping Access absence, exact token-name and matching
  row counts `0 / 0`;
- OmniRoute `team-rollout-test` key absence; and
- exact `mysw.me` zone, `ai-api-omniroute.mysw.me` target, fixed rule/token
  values, specific-zone resource, and exactly `Zone WAF Edit` plus `Zone Read`.

The separate mandatory action-time user confirmation immediately before final
Cloudflare Create plus user-native semantic Copy and masked Paste remains. The
separate confirmation before exact-row deletion remains. Standing authority
and static PASS waive neither. The fixed sole owner process, same-process
masked input, bounded R5 child/owner deadlines, universal post-accept
revocation hold, refreshed row counts `0 / 0`, retained-token HTTP `401`,
guarded cleanup, redaction, no retry/fallback/handoff, and all excluded public,
DNS, Tunnel, Access, key, model, routing, VM-power, permission, evidence, and
unrelated deletion actions remain unchanged.

## Static acceptance

Independent direct-byte Sol High review must verify:

- strict UTF-8 without BOM, LF-only, one trailing LF, index `0`, unrelated
  baseline `12`, and only this exact ignored-path artifact newly written;
- exactly one JavaScript fence parses as a module without evaluation;
- all old identifiers are guarded with `typeof` before any possible reference;
- exact old counters `1/1`, documentation `1/1`, shape `true`, error `NONE`,
  and old exact boolean `true` are required;
- exactly one `tabs.list()` and one `nodeRepl.write`, with the write wrapping
  the sole fixed terminal object; zero bare terminal object expressions;
- only `Array.isArray` and `.length` touch the temporary list, which is nulled
  in `finally`; no element access, iteration, metadata, object, or exception
  output;
- every new executable name has the unique
  `secureConsoleRetainedRecoveryV1` prefix;
- every prohibited operation count is zero; and
- manual blank-tab closure, full revalidation, mandatory confirmations,
  preserved lifecycle/deadlines/redaction/exclusions, complete-forwarding
  rule, one-shot failure boundary, and `authorizes_live_execution=false` are
  explicit.

Static checks must not import or evaluate the cell and must perform no live
action. Only this recovery's future committed bytes, fresh independent PASS,
and exact action-time pins can make the one cell eligible. This document itself
remains static and non-executing.
