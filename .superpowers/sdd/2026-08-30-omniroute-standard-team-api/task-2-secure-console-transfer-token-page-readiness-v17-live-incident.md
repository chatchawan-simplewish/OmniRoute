# OmniRoute V17 explicit evaluator-timeout readiness — live incident

Date: `2026-09-02` (`Asia/Bangkok`)

## Disposition

- `v17_consumed=true`
- `v17_pass=false`
- `v17_retry_authorized=false`
- `provider_mutation_performed=false`
- `secret_action_performed=false`
- `exact_created_tab_closed=true`
- `residue_converged=true`
- `retained_v17_handle=false`

V17 is permanently spent. It must never be retried, continued, reinterpreted,
or used as evidence for a later PASS.

## Exact reviewed execution chain

- V16 consumed-failed-clean incident:
  `eab7c640add2e0ed5fd5bc22376cd93b5cb2b138`.
- V17 brief: `df60f78c2d5aaa354b2d2cfe5e4224307add1e38`.
- Independent Sol High PASS review:
  `7d749d597885b6e832593f4f5c6ed0063c52f54c`.
- Non-self-referential classification:
  `823b785bd89ed111158dae18099e2344992700e6`.
- Executed payload: `31261` bytes, SHA-256
  `6D21204A07C427585C3DE5D8F43F9592A11F2C7E79ECC9DC901F31034AB3EC0E`.

The post-commit tuple and every fresh action-time pin passed. The source
projection remained `10661` records at SHA-256
`C4C85893769D133224AC7404B628C8B0D4B0EBBFF471620B158E652E152B4EBD`;
the index was empty; the exact 12-path dirty baseline was preserved; runtime,
evidence-worktree, residue, VM1205, DNS, and persistent V16-through-V4 pins were
exact; and every V17 declaration was absent.

## Sanitized result and falsified hypothesis

V17 reproduced V16's exact safe failure boundary:

- declaration, predecessor, controller/handle shape, navigation, unique visible
  search input, exact baseline validation/semantics, and the one fixed
  non-secret filter fill passed;
- terminal evaluation was attempted once but did not fulfill;
- terminal validation and every later query-echo/Create/name/row read were not
  reached;
- sanitized error class was exactly `Error`;
- the exact created tab closed once and close settled; and
- V17 is consumed, ineligible, clean, null, with exact V16 and V4 tuples true.

V17 added the supported locator evaluator option `{ timeoutMs: 25000 }` around
the unchanged `20000 ms` page-local terminal loop, under the unchanged
`60000 ms` outer deadline. The identical attempted/not-fulfilled result proves
that omission of this transport option was not sufficient to explain or fix the
rejection. No retry or further browser inspection is authorized by this
incident.

No title, broad page text, row text, fingerprint, URL, DOM, HTML, screenshot,
clipboard content, token, credential, or secret was emitted.

## Next boundary

Only offline diagnosis and a fresh independently reviewed replacement are
permitted. The replacement must treat V16 and V17 as consumed failed-clean and
must not merely repeat the evaluator with another timeout. It must preserve all
semantic, stability, fixed-output, cleanup, secret, confirmation, and
no-residue constraints.

`authorizes_live_execution=false`
