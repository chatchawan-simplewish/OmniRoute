# OmniRoute V34 fresh-session attachment and account-home reacquisition — execution classification

Date: `2026-09-02` (`Asia/Bangkok`)

`authorizes_live_execution=false`

## Classification

**STATIC PASS — Call 1 is eligible only after every Call-1 action-time pin
revalidates exactly. Call 2 remains separately ineligible until exact Call-1
PASS and every Call-2 action-time pin revalidate.**

This is a non-self-referential evidence classification. It records no commit,
byte length, hash, blob, or HEAD claim for itself and grants no independent
provider authority. Its direct parent must be independent Sol High PASS review
`0c5a375c9ed161e5a8dea69762d43f7af455f364`. After committing this file, the
coordinator must separately record the classification identity and complete
post-commit tuple before any Node call.

## Immutable reviewed package

- V33 action-time runtime/controller drift incident:
  `60358392f188fe3706c36aa518c9947a21e8ba7b`. V33 was never executed and is
  permanently ineligible.
- Corrected V34 brief commit:
  `de938a548b036e7b83af4b4168066b0e6cd25066`; direct parent is the original
  V34 FAIL review `964eaf4be28cf5fe8936e1ec30a3a8f72425c2eb`.
- Corrected brief path:
  `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-secure-console-transfer-v34-fresh-session-attachment-account-home-reacquisition-brief.md`.
- Corrected brief: `21185` bytes; SHA-256
  `1D0B29390AE70279E4C462E687F667B9A3384A0052E66F8E2C1FA68904E68D44`;
  Git blob `848ea19d600f15e879f7e48d7b459d88e49231f4`.
- Reviewed normalized LF Call 1: `5473` UTF-8 bytes; SHA-256
  `E529B443FD20595772A56C7BC6E66B8203604FD168C21195DE31E1A05646D637`.
- Reviewed normalized LF Call 2: `8224` UTF-8 bytes; SHA-256
  `5934E2C76F37B1F76490FD06B0EF59E12447153F6567DC3BA3590974BBD932EA`.
- Independent fix-round-1 Sol High PASS review commit:
  `0c5a375c9ed161e5a8dea69762d43f7af455f364`; direct parent
  `de938a548b036e7b83af4b4168066b0e6cd25066`.
- PASS review path:
  `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-secure-console-transfer-v34-fresh-session-attachment-account-home-reacquisition-fix1-sol-review.md`.
- PASS review: `9029` bytes; SHA-256
  `7C6311F62160D5B72E6AFA8262B8171662378BAA9527D5E937D49326D7D5BF17`;
  Git blob `dfff29fd8e6fcec70ff410732d1d39641c6e3b9e`.
- Review verdict: PASS; unresolved Critical `0`, HIGH `0`, IMPORTANT `0`,
  Minor `0`; `authorizes_live_execution=false` pending this classification
  and action-time pins.
- Expected classification path:
  `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-secure-console-transfer-v34-fresh-session-attachment-account-home-reacquisition-execution-classification.md`.
- Stable retained source projection: `10661` records; SHA-256
  `C4C85893769D133224AC7404B628C8B0D4B0EBBFF471620B158E652E152B4EBD`.

The original V34 review remains immutable FAIL evidence. Its findings are
closed only by the corrected brief and independent fix-round-1 PASS review;
the original verdict is not overwritten, retried, or reinterpreted.

## Exact bounded semantics

Call 1 is the only fresh attachment cell. It consumes before any import or
connection action, imports the exact pinned current Chrome runtime once, calls
setup once, calls `browsers.get("chrome")` once, obtains the complete current
documentation once, and writes only that documentation plus one fixed terminal
object. It performs no tab operation. Exact PASS retains only the reviewed V34
runtime/controller bindings. A JavaScript-visible write failure clears them and
rethrows without a second output. Unobservable output transport uncertainty
requires disposal of the entire fresh Node realm.

Call 2 is a separate one-shot cell. It consumes before any precondition or tab
action and requires exact persistent Call-1 PASS. It calls `tabs.selected()`
once, navigates that exact externally owned selected task tab once to the
Cloudflare root, waits once for `20000` milliseconds, reads its URL once, and
evaluates one fixed body projection. PASS requires exact private account-home
host/path, exactly one same-account `mysw.me` zone href, a positive anchor
count, and zero busy markers. It retains only that exact selected handle after
full PASS and never closes the externally owned tab.

Neither call may list tabs or browsers, reacquire by ID or URL, reconnect,
retry, fall back, create or close a tab, click, press, fill, serialize DOM,
capture a screenshot, use clipboard state, mutate a provider, expose a secret,
start a process, mutate VM/network/routing state, or relax a verdict. The final
Create/native Copy/native masked Paste confirmation and later separate exact-row
deletion confirmation remain mandatory and unreached.

## Call-1 action-time pins

Immediately before the sole Call-1 Node invocation, the coordinator must
require all of:

- HEAD equals this classification commit; its sole direct parent equals
  `0c5a375c9ed161e5a8dea69762d43f7af455f364`; and the commit changes only the
  expected classification path;
- a separately recorded post-commit tuple pins this classification's commit,
  parent, path, bytes, SHA-256, blob, current HEAD, chain/exclusion counts,
  stable projection, empty index, and exact inherited 12-path dirty baseline;
- the exact corrected brief, both executable cells, PASS review, parent chain,
  direct-byte pins, and zero-finding verdict above;
- exact current Chrome module and API bytes, required API declarations, clean
  evidence worktree, zero temporary/process residue, VM1205 safe checkpoint,
  and absent public DNS;
- the owner's exact confirmation that Chrome Profile
  `Codex-Chrome-Bell-PC2`, the intended window, and intended task tab are the
  only selection offered to the extension;
- a fresh Node realm with no predecessor controller and every V34 declaration
  absent; and
- extraction of Call 1 from the committed corrected brief with its exact
  reviewed normalized byte count and SHA-256.

The first Node call receiving Call 1 consumes it before any precondition. An
action-time mismatch stops before that call and does not consume Call 1.

## Call-2 action-time pins

Call 2 remains ineligible unless Call 1 has exact complete PASS output and
completed tool status. Immediately before the sole Call-2 Node invocation, the
coordinator must revalidate every immutable local, runtime, worktree, residue,
VM, DNS, and profile-selection pin; exact persistent Call-1 counters, state,
and bindings; absent Call-2 declarations; and extraction of exact reviewed
Call-2 bytes and SHA-256.

The first Node call receiving Call 2 consumes it before any precondition or
selection. Any mismatch stops before that call and does not consume Call 2.

## Terminal interpretation

Call-1 PASS requires exact result
`EXACT_V34_FRESH_CHROME_ATTACHMENT_PASS`, every declared shape and validation
boolean true, all attempted/fulfilled pairs `1/1`, consumed and attachmentExact
true, state `V34_ATTACHMENT_PASS`, error class `NONE`, complete documentation
output, complete fixed terminal output, and completed tool status.

Call-2 PASS requires exact result
`EXACT_V34_SELECTED_ACCOUNT_HOME_REACQUISITION_PASS`, every declared validation
and semantic boolean true, exact fixed snapshot shape, every attempted/fulfilled
pair `1/1`, error class `NONE`, consumed and bindingEligible true, bindingNull
false, state `V34_ACCOUNT_HOME_READY_ELIGIBLE`, complete fixed terminal output,
and completed tool status.

Anything else is failed, uncertain, malformed, incomplete, or residue-bearing.
Record only fixed sanitized evidence, classify that call spent, and stop. Never
retry, continue, reinterpret, reconnect, fall back, manually integrate, or
relax either V34 call.
