# OmniRoute V39 fresh-session cross-realm task-tab reacquisition — execution classification

Date: `2026-09-02` (`Asia/Bangkok`)

`authorizes_live_execution=false`

## Classification

V39 is a fresh-session, one-shot, no-retry reacquisition gate. It may be
considered for one live send only after a separate post-commit coordinator
tuple and every action-time pin below pass. This artifact is
non-self-referential: it makes no claim about its own commit, blob, byte count,
digest, repository `HEAD`, or post-commit ancestry.

## Immutable reviewed package

- Brief commit:
  `f697a8770b6456324c05b088545c1380a4b9f495`.
- Brief path:
  `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-secure-console-transfer-v39-fresh-session-cross-realm-task-tab-reacquisition-brief.md`.
- Brief bytes / SHA-256 / blob:
  `21821` /
  `A317EFC5F3435212D8E223E2AF46C6CC043CD4129918A0889FF20213CDD16648` /
  `4753fbace65642c725e4d91e3717f99797ff9f04`.
- Pure-fixture path:
  `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-secure-console-transfer-v39-pure-fixtures.mjs`.
- Fixture bytes / SHA-256 / blob:
  `10435` /
  `FA5C0750EAF74E01491B31695EE53C26441F207F49823C83CEBDAD00E8C918E2` /
  `2ead83330183502708d46c45e2524bd57ad11db1`.
- Sole LF executable bytes / SHA-256:
  `16848` /
  `B4129373982168BE96B70E528FB1029977050A6E9630D2FF7B940B033BEF9CF4`.
- Fixture terminal:
  `{"result":"V39_PURE_FIXTURES_PASS","briefBytes":21821,"briefSha256":"A317EFC5F3435212D8E223E2AF46C6CC043CD4129918A0889FF20213CDD16648","executableBytes":16848,"executableSha256":"B4129373982168BE96B70E528FB1029977050A6E9630D2FF7B940B033BEF9CF4","syntax":"PASS","moduleShape":true,"foreignRealmAccepted":true,"uniqueCandidateEnforced":true,"fullCellSuccess":true,"fixedFailure":true,"getterCalls":0}`.
- Independent Sol High PASS review commit:
  `68b29307f925e56a2f03053a94f3683b5aa91e53`.
- Review path:
  `.superpowers/sdd/2026-08-30-omniroute-standard-team-api/task-2-secure-console-transfer-v39-fresh-session-cross-realm-task-tab-reacquisition-sol-review.md`.
- Review bytes / SHA-256 / blob:
  `12042` /
  `A6B71A66DBAE9B6471DA8F5DBAD6FEC69D0CF6E0DA4920373D6EB40934A9144E` /
  `b7c5815b4bc91546055f04853fbae3e72da60c79`.
- Review findings: Critical `0`, HIGH `0`, IMPORTANT `0`, Minor `0`.

## Consumed predecessor boundary

V3 is consumed and failed. V4 is static evidence only. V34 Call 2, V35, V36,
V37, and V38 are consumed and may never be retried, continued, reinterpreted,
used as fallback, or treated as inherited browser state. V38's exact live
terminal is diagnostic evidence only: array true, local prototype identity
false, offered count `9`, and every bounded descriptor/record/value predicate
true, with zero claim or navigation. Its realm was reset.

## Exact bounded semantics

The committed sole cell becomes consumed before import. It has exactly one
dynamic import, runtime setup with no options, browser get, complete
documentation read/write, session name, `openTabs`, unique candidate
classification, `claimTab`, account-home navigation, fixed wait, URL read, and
bounded semantic snapshot. It has no retry, fallback, selected/list/get,
reconnect, new/close-tab, provider, clipboard, credential, DNS, routing, VM,
listener, proxy, or owner action.

The listing must satisfy `Array.isArray`, zero symbols, the exact bounded own
length/name/index-descriptor contract, and the exact bounded plain-record key,
descriptor, and string contract. It deliberately does not require local
`Array.prototype` identity. Exactly one candidate must have an exact safe HTTPS
Cloudflare origin and `/<32 lowercase hex>/mysw.me` path; zero or multiple
candidates stop before claim. No tab metadata or raw error is emitted.

After one successful claim, the tab must prove exact controller identity and
the unchanged Cloudflare account-home URL/signature: exact account home path,
exactly one `mysw.me` zone anchor, positive bounded anchor count, and zero busy
markers. Exact counters must all equal one. Success retains only the reviewed
runtime/browser/tab bindings and marks the tab eligible for a later separately
reviewed step in the same realm. Failure clears every binding. The unbound
outer `catch` assigns only literal `Error`; terminal-write failure repeats
cleanup and rethrows.

Success authorizes no provider mutation or secret action. Failure, rejection,
timeout, terminal-write failure, or transport uncertainty consumes V39 and
requires immediate realm disposal. No retry, continuation, verdict relaxation,
or fallback is permitted.

## Action-time pins

Immediately before the sole live send, the sole Sol High owner must prove:

- exact brief, fixture, executable, review, and classification ancestry,
  bytes, digests, and blobs;
- the exact fixture PASS terminal and executable syntax;
- runtime module
  `C:\Users\chatc\.codex\plugins\cache\openai-bundled\chrome\26.831.20005\scripts\browser-client.mjs`
  is `149771` bytes with SHA-256
  `A50E341988B45C547B02DB74787AA64AFFC64F42B5C605308823EEC853179298`;
- API documentation
  `C:\Users\chatc\.codex\plugins\cache\openai-bundled\chrome\26.831.20005\docs\api.json`
  is `58480` bytes with SHA-256
  `A7C8D53096EA2563CF939D5F7A37DFC2AE0701E891510566930868D04CE0F5DF`;
- a separate post-commit tuple proves the classification parent, one-path
  commit diff, classification metadata, chain/exclusion counts, stable
  `10661`-record ASCII projection digest
  `C4C85893769D133224AC7404B628C8B0D4B0EBBFF471620B158E652E152B4EBD`,
  empty index, and exact twelve-path product baseline;
- evidence worktree clean at
  `80adaa7d5d63d1d2c7bfa63b236c6bee93b3b1d8`;
- zero Windows secure-console residue, absent public A/CNAME at both required
  resolvers, and the unchanged VM1205 no-listener/no-proxy/no-tunnel checkpoint;
- prior exact owner archived and no competing live-resource/authority owner;
- exact owner confirmation:
  `Chrome Profile Codex-Chrome-Bell-PC2, intended window, and intended task tab are selected; no other Chrome profile/window is offered to the extension.`;
- a freshly reset Node realm with every named V35-V39 declaration absent; and
- extraction of the exact committed sole cell with matching bytes/hash/syntax
  immediately before its only send.

Any mismatch is a fail-closed stop before the live send.

## Preserved later constraints

All V4 semantic page-signature, completeness, no-residue, no-retry, secret,
confirmation, and cleanup constraints remain binding. No secret may be printed,
committed, logged, or placed in review evidence. The mandatory final
Create/native Copy/native masked Paste confirmation and the later separate
exact-row deletion confirmation remain external, unreached, mandatory, and
cannot be pre-approved, automated, delegated, or waived.

This classification permits only action-time revalidation and the coordinator
tuple. It does not itself authorize a live send.

`authorizes_live_execution=false`
