# OmniRoute V43 fresh-realm token-page readiness — execution classification

`authorizes_live_execution=false`

## Classification

V43 is the smallest fresh-session, one-shot, no-retry replacement that combines
Chrome task-tab attachment, account-home validation, transfer into fresh outer
bindings, and read-only Cloudflare API-token-page semantic readiness. It may be
considered for one live send only after this file is committed alone on the
independent PASS review, a separate post-commit coordinator tuple is recorded,
and every action-time pin below passes.

This classification is non-self-referential. It makes no claim about its own
commit, parent, bytes, digest, blob, current `HEAD`, projection, or runtime
state. Those facts belong only in the later coordinator tuple.

## Immutable reviewed package

- Reviewed fix2 commit:
  `58b64d3cc97a62668e9ff420a12dfce2fd148426`.
- Brief: `6881` bytes; SHA-256
  `E2BFD7C00A77A282BA1BB30FA0C0F9DD5F3FC05A593234D6A759C8429A4132C1`;
  blob `a900ae26269863ae5083a7465d726b4f599f2bae`.
- Executable: `39193` bytes; SHA-256
  `13E56326548D660B111DDC63857BFD33250A548F7D094FA4131EEBB694D24B01`;
  blob `a1d4f0cb1351fa4c11714c56e008958dabd6ca55`.
- Pure fixture: `45511` bytes; SHA-256
  `4BAE1CE107930217EF5E171D6FD9704AC6D4EE87C9D6FAE365C57CC88FD496A5`;
  blob `56b59b2242168f0395a89f3cd6d204b43c1ac1ae`.
- Fixture terminal:
  `{"result":"V43_PURE_FIXTURES_PASS","briefBytes":6881,"briefSha256":"E2BFD7C00A77A282BA1BB30FA0C0F9DD5F3FC05A593234D6A759C8429A4132C1","executableBytes":39193,"executableSha256":"13E56326548D660B111DDC63857BFD33250A548F7D094FA4131EEBB694D24B01","syntax":"PASS","declarationFree":true,"listingMatrix":true,"exactOutputKeys":true,"fullCellSuccess":true,"fixedFailureCleanup":true,"hostileThrownValues":true,"terminalOutputCleanup":true,"completeCounterVectors":true,"getterCalls":0,"listingGetterCalls":0,"outputGetterCalls":0}`.
- Independent Sol High PASS review commit:
  `a3603b212749d4b3a2195dadfb8d7c126ee70b47`.
- PASS review: `8879` bytes; SHA-256
  `8EA537385E2E9E2F2ED34FDE267AE6C3CA5C883B1C8C09C4277289F3DBBBB2C5`;
  blob `308e563ee51248bb53c3463cb532638120960f03`.
- Review findings: Critical `0`, HIGH `0`, IMPORTANT `0`, Minor `0`.

## Exact one-shot semantics

V3 is consumed and failed. V34 through V41 are consumed. V42 is superseded and
static. None may be retried, continued, reinterpreted, or used as fallback.
V43 is initially unconsumed and consumes before predecessor validation, module
import, browser connection, tab enumeration, claim, or navigation. Any failed,
uncertain, interrupted, or output-failed live V43 send permanently spends V43.

The reviewed cell requires a newly reset, declaration-free persistent Node
realm. It performs exactly one runtime import/setup, Chrome connection,
documentation read, session naming call, `openTabs`, and rank-zero cached-ID
claim. It validates the bounded cross-realm listing by own descriptors, accepts
only an exact Cloudflare URL shape, never inspects later record values, claims
only the cached primitive rank-zero ID, navigates to account home, waits once,
reads the URL once, and validates one bounded account-home signature.

Only after that attachment succeeds does V43 transfer the adopted controller
into its fresh outer binding. It then navigates once to the API-token page,
validates the exact token-page signature, fills only the fixed non-secret filter
`OmniRoute secure console R5 20260901`, and performs read-only exact Create/name/
empty-row checks. It does not click Create, copy, read a token, access the
clipboard, create or mutate provider state, or handle a secret.

Success retains only the reviewed V43 continuation tab and boolean/state flags.
Runtime, setup, agent, Chrome, attachment, offered-list, evidence, raw error,
account, URL, DOM, and other broad intermediates are cleared. Every failure,
including documentation-output or final-output failure, nulls both attachment
and outer bindings, marks them ineligible, retains only fixed evidence, and
rethrows terminal-output failure by identity without inspecting it.

## Action-time pins

Immediately before the sole V43 live send, the coordinator must prove all of:

- this classification is the only changed path in its commit and its direct
  parent is the independent PASS review commit above;
- the separate post-commit tuple records current `HEAD`, classification path,
  direct bytes/SHA-256/blob, parent, chain/exclusion counts, empty index, exact
  12-path dirty product baseline, and the stable `10661`-record ASCII projection
  SHA-256 `C4C85893769D133224AC7404B628C8B0D4B0EBBFF471620B158E652E152B4EBD`;
- the installed runtime module remains `149771` bytes / SHA-256
  `A50E341988B45C547B02DB74787AA64AFFC64F42B5C605308823EEC853179298`
  and its documentation remains `58480` bytes / SHA-256
  `A7C8D53096EA2563CF939D5F7A37DFC2AE0701E891510566930868D04CE0F5DF`;
- the evidence worktree is clean at
  `80adaa7d5d63d1d2c7bfa63b236c6bee93b3b1d8`, Prox-01 and VM1205 remain
  online, and the reviewed ownership, DNS, no-residue, and no-background-process
  pins remain exact;
- a new CUA reset is followed only by the tool-mandated one-call
  `await cua.getState();` initialization and a fixed state-only declaration
  audit; after that initialization there is no browser-client reconnection,
  `openTabs`, claim, navigation, or provider action before the committed V43
  cell; and
- the owner freshly confirms at action time exactly:
  `Chrome Profile Codex-Chrome-Bell-PC2, intended window, and intended Cloudflare task tab are selected; no other Chrome profile/window is offered to the extension.`

Any mismatch stops fail-closed and spends no substitute gate. There is no retry,
fallback, override, manual integration, or verdict relaxation.

## Preserved later constraints

All V4 semantic page-signature, completeness, no-residue, no-retry, secret,
confirmation, and cleanup constraints remain binding. The mandatory final
Create/native Copy/native masked Paste confirmation and the later separate
exact-row deletion confirmation remain unreached, external, and non-delegable.

This artifact permits only the remaining coordinator validation. It does not
itself authorize a live browser send, provider action, or gate consumption.

`authorizes_live_execution=false`
