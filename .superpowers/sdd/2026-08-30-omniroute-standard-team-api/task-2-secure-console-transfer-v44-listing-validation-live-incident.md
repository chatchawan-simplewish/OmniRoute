# OmniRoute V44 listing-validation live incident

Date: `2026-09-02` (`Asia/Bangkok`)

V44 is **consumed / failed / cleanup complete / static evidence only**. Never
execute, retry, continue, reinterpret, or use it as fallback.

## Reviewed package and action-time state

- Candidate fix commit: `3fed766dd`.
- Independent Sol High PASS review commit:
  `0dc21f0ee82c0d4b90f50f6af82e829623dd1365`.
- Non-self-referential classification commit:
  `81febb1267a4b9f5a8aa1953e606dae87c20e2f6`.
- Post-commit tuple: parent
  `0dc21f0ee82c0d4b90f50f6af82e829623dd1365`, one classification path,
  classification `4549` bytes / SHA-256
  `8FFE689B801C7263C62884C06F2B7E2AB0906977D5C016072C7325952E82F82A` /
  blob `a6919be0fba237f42919de6d8d2a130cba8e36b1`, chain/exclusions
  `189 / 191`, `10661` projection records at SHA-256
  `C4C85893769D133224AC7404B628C8B0D4B0EBBFF471620B158E652E152B4EBD`,
  empty index, and the exact 12-path dirty product baseline.
- Candidate/runtime/documentation pins, evidence worktree, public DNS absence,
  secure-console temporary/process residue, sole ownership, and VM1205 safe
  checkpoint all passed immediately before the reset.
- After the reset, the owner supplied the required exact Chrome selection
  confirmation. Tool-mandated `await cua.getState();` showed the sole extension
  Chrome profile `Codex-Chrome-Bell-PC2` and the intended Cloudflare API Tokens
  tab at rank zero. A fixed direct-`typeof` audit proved all `73` V35-V44 names
  undefined.

## One-shot result

The exact committed V44 cell was sent once. The attachment stage consumed V44,
imported and initialized the exact pinned runtime, connected to Chrome, validated
the complete `42370`-character documentation, emitted that documentation,
named the session, and called `openTabs()` exactly once.

The returned listing did not satisfy V44's pure trusted-listing shape. The cell
reported `offeredCount=-1`, `listingValidated=false`,
`rankZeroSelected=false`, `candidateValidated=false`, and
`candidateUrlCloudflare=false`. It stopped before `claimTab()`.

Counters at the stop were:

- import/setup/connect/documentation/documentation-output/session-name/openTabs:
  each attempted `1`, fulfilled `1`;
- claim/navigation/wait/URL/snapshot: each attempted `0`, fulfilled `0`;
- token-page binding/navigation/readiness/fill/Create-read/name-read/row-read:
  each attempted `0`, fulfilled `0`;
- final redacted evidence write: attempted `1`.

The attachment and outer result labels were respectively
`V44Attachment_FRESH_CROSS_REALM_TASK_REACQUISITION_FAILED_STOP` and
`V44_TOKEN_PAGE_SEMANTIC_READINESS_FAILED_STOP`. Both bindings were null and
ineligible. `predecessorRuntimeCleared=true` and
`failureCleanupComplete=true`. The CUA realm was reset immediately afterward.

## Security and provider boundary

No tab was claimed or navigated. No Cloudflare page read or field fill occurred.
No Create control was clicked, no token was created or copied, no clipboard or
secret operation occurred, and no provider-persistent mutation occurred. The
mandatory final Create/native Copy/native masked Paste confirmation and later
separate exact-row deletion confirmation remain unreached.

The failure proves only that the V44 trusted-listing predicate rejected the
single returned `openTabs()` value. It does not prove which descriptor or record
shape differed, and no cause is inferred from the redacted result.

## Replacement boundary

A replacement requires a new gate identity and a new independently reviewed
contract. It must preserve all V4/V44 page-signature, rank-zero selection,
completeness, no-residue, no-retry, secret, confirmation, counter, output, and
cleanup constraints. It may add only bounded, read-only structural evidence
needed to classify the listing shape without revealing tab titles, URLs,
identifiers, or other provider data. V44 remains permanently spent.

`authorizes_live_execution=false`
