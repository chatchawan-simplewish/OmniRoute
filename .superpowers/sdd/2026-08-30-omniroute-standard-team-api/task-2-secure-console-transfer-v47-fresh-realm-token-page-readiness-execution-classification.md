# V47 fresh-realm token-page readiness execution classification

Date: 2026-09-02 (Asia/Bangkok)

## Classification

`V47_CLASSIFICATION=ONE_SHOT_LIVE_ELIGIBLE_ONLY_AFTER_ACTION_TIME_PINS`

This document classifies the immutable corrected V47 package after independent
Sol High PASS review. It does not itself execute or consume V47 and contains no
self-referential commit identifier.

## Authoritative lineage

- V46 consumed-failure incident:
  `0b7944703a1b0219b3c37707a9ff8030c25d9028`.
- Initial V47 candidate:
  `ea7830005a32b71e26ffa63a9a6d61cf3ddcd71c`.
- Independent V47 FAIL review:
  `678bbbcede8a11b0a379511eda7cf42db22755e5`.
- Corrected V47 candidate:
  `5b34ce5b4dd4409e8e21569615138181ddc7c2c5`.
- Independent corrected V47 Sol High PASS review and required classification
  parent: `43c5b56feb23756ba37551c54aa4a3157376bb3b`.

The PASS review has zero Critical, HIGH, IMPORTANT, and Minor findings and
states `authorizes_live_execution=false` until this separate classification,
the post-commit coordinator tuple, and all action-time prerequisites are
complete.

## Exact reviewed package

- Brief: `7446` bytes; SHA-256
  `E404CD3FA2F0DA28209CCB21FD53087BCAC59C0434649FAA2F72013A94FDFF83`;
  blob `91c4954fac980da63586cc8a8856d5c59bea83d5`.
- Executable: `41533` bytes; SHA-256
  `5B44CE8AADCA857C24BCCC6C93F566856AE93711065B8BA4577590703DAA708D`;
  blob `087c3ec28c414abc66dd5db0a58405fb635061f4`.
- Pure fixture: `50343` bytes; SHA-256
  `90771E3BB4423B986A8AAF5CD6F4BD6F208B176639D1BBCBF6BEB466F24815A3`;
  blob `6efab96fadc7e1b4613b1db7a3db38a120af6d86`.
- PASS review: `10441` bytes; SHA-256
  `CC346669C48C35DC494B64D181A0871C7E4DF6C660B0EACBE140E488E500B65A`;
  blob `232572fadad5fdb5ae1f96549b5e5a7f19ad57d8`.

Any path, byte length, digest, blob, parent, runtime, documentation, worktree,
ownership, DNS, VM, process, listener, temp-state, or browser-selection drift
invalidates execution eligibility.

## Approved scope

V47 may be consumed once only to:

1. create fresh V47 declarations after a direct state audit proves all 94
   V35-V47 predecessor and candidate declaration names absent;
2. import the pinned browser client once, create one fresh runtime/controller,
   connect to the sole offered Chrome profile, validate complete pinned
   documentation, name the session, list offered task tabs once, validate and
   claim the exact rank-zero returned record once;
3. navigate once to Cloudflare account home and prove the preserved account and
   `mysw.me` zone semantic signature;
4. navigate once to the exact API Tokens page;
5. wait once for the existing search textbox to become visible before checking
   its unique count and dependent page structure; and
6. prove the preserved complete token-page semantics using only the fixed
   non-secret search text and bounded fixed-schema output.

V47 counts Create but never clicks it. It cannot create, copy, paste, expose,
store, transmit, revoke, or delete a credential; mutate Cloudflare, DNS,
routing, VM, filesystem, clipboard, browser storage, cookies, or tabs; reconnect;
retry; fall back; override; relax a verdict; or manually continue after failure.

## Action-time prerequisites

Immediately before any V47 live attempt, the sole owner must prove:

- branch/worktree/HEAD lineage and all candidate/review/classification bytes;
- classification is the only changed path on the exact PASS-review parent;
- empty index and exact 12-path dirty product baseline;
- exact commit-chain/exclusion counts and unchanged outside projection;
- installed browser runtime and documentation bytes still match reviewed pins;
- evidence worktree is clean at its pinned commit;
- public A/CNAME absence for `ai-api-omniroute.mysw.me` remains exact;
- zero secure-console temp/process/listener/owner/proxy residue;
- Prox-01 and VM1205 are online and the VM1205 safe checkpoint remains exact;
- the current task remains sole worktree and one-shot authority-gate owner; and
- no prior V47 attempt or declaration exists.

Only after those proofs may the CUA realm be reset. After reset, the owner must
freshly confirm exactly:

`Chrome Profile Codex-Chrome-Bell-PC2, intended window, and intended Cloudflare task tab are selected; no other Chrome profile/window is offered to the extension.`

That confirmation cannot be reused. The first fresh-realm call must be exactly
`await cua.getState();` and must prove the sole offered Chrome profile plus the
rank-zero intended Cloudflare API Tokens tab. One following fixed, direct
`typeof` state-only audit must prove all 94 V35-V47 names absent. No browser
reconnect, listing, claim, navigation, page read, or provider action may occur
between initialization and the exact reviewed V47 executable.

## Terminal handling

The exact reviewed V47 executable may be sent once. Any failure, timeout,
uncertainty, exception, drift, unexpected output, or cleanup doubt permanently
spends V47 and stops. There is no retry, continuation, fallback, override,
verdict relaxation, reuse, or manual integration.

On exact PASS, only the retained V47 owned-tab binding and eligible state may be
used, and only by a separately reviewed next step. On failure or uncertainty,
run only one fixed state-only cleanup proof, reset the realm, commit sanitized
evidence, and stop.

## Preserved external confirmations

Every V4/V44/V46/V47 page-signature, completeness, counter, no-residue,
no-retry, secret, output, and cleanup constraint remains binding. The final
Create/native Copy/native masked Paste confirmation remains mandatory at action
time. The later exact-row deletion confirmation remains separate and mandatory.
Standing unattended authority does not waive either confirmation.
