# V49 fresh-realm token-page readiness executable brief

## Authority and disposition

This brief implements the independently approved V49 design at commit
`6fd3669ce` and its zero-finding Sol High design review at commit `582968408`.
It does not authorize live execution.

V47 and V48 are consumed and permanently ineligible. Neither may be retried,
continued, reused, reinterpreted, or relaxed. V47's exact historical listing
failure remains **NOT PROVEN**. V49 relies only on V48's first fixed sanitized
diagnostic payload plus the eventual realm reset; V48's corrected second
cleanup query is non-authorizing incident evidence and no precedent.

## Exact source and candidate pins

- V46 reviewed executable SHA-256 retained by the V47 fixture lineage:
  `55C8833CF0E405022B747298CE71E7ED6DCF4A301A6C429026A598004444050E`
- V47 source executable length: `41533` bytes.
- V47 source executable SHA-256:
  `5B44CE8AADCA857C24BCCC6C93F566856AE93711065B8BA4577590703DAA708D`
- V47 source fixture length: `50343` bytes.
- V47 source fixture SHA-256:
  `90771E3BB4423B986A8AAF5CD6F4BD6F208B176639D1BBCBF6BEB466F24815A3`
- Executable length: `41699` bytes.
- Executable SHA-256:
  `F279984ABAEDD5087B99956E04763F592022E45E2D3AE974293184F148BD12AE`
- Pure fixture length: `50618` bytes.
- Pure fixture SHA-256:
  `8C916F69588C9003CEB4FF2C0B0C28EEA75D4557FAA8A03BFB9B8EA9552F7B7F`

## Exact implementation delta

The executable is the final V47 executable mechanically renamed to V49, with
only three new predecessor-absence checks:

- `secureConsoleV48Consumed`
- `secureConsoleV48State`
- `secureConsoleV48Result`

The fixture is the final V47 fixture mechanically renamed to V49, with only:

- the same three V48 predecessor-contamination cases;
- one explicit assertion that an absent optional key may pass;
- one present-`undefined` optional-key rejection case; and
- the terminal predecessor field renamed to `v48PredecessorsAbsent`.

Every present documented listing value remains a bounded, non-empty,
control-free string. Present `undefined` is rejected before claim. The exact
returned rank-zero record is claimed once only after every strict V47 listing
predicate passes.

## Preserved one-shot behavior

V49 is consumed before validation or import. In a fresh realm it performs at
most one pinned import, setup, Chrome connection, complete documentation read
and write, session name, `openTabs`, exact-record claim, each reviewed
navigation, each reviewed URL read, and each reviewed semantic snapshot.

It preserves the account-home signature, exact Cloudflare token-page URL,
unique visible `search api tokens` textbox, bounded `aria-controls`, unique
controlled root, visible paginator, empty baseline, fixed non-secret filter
`OmniRoute secure console R5 20260901`, exact query echo, terminal `0-0 of 0`,
zero rows, disabled paginator controls, zero busy state, exactly one Create
Token control, and zero exact token-name or matching-row occurrences.

The four downstream readiness attempts and fulfillments remain search-control
visibility, paginator visibility, query echo, and empty status. All inherited
counter values and completeness predicates remain exact.

Create is counted but never clicked. The executable contains no Copy,
clipboard, token-secret, credential, storage, cookie, DNS, routing, VM mutation,
provider mutation, tab creation/close, reconnect, retry, fallback, override,
verdict relaxation, or manual-continuation path.

## Output and cleanup

Only fixed literals, booleans, bounded approved counts, exact counters, and the
fixed-schema attachment result may be emitted. Raw listings, descriptors,
records, identifiers, titles, URLs, account segments, DOM text, thrown values,
credentials, tokens, and secrets are never emitted or retained.

Exact PASS retains only the owned V49 token-page binding plus its minimal
eligibility/state/consumption flags. All broad runtime, browser, documentation,
listing, descriptor, record, URL, and snapshot aliases are cleared.

Any failure or uncertainty spends V49, clears both tab bindings and every broad
alias, and stops ineligible. Only one fixed state-only cleanup proof is allowed;
if it fails there is no corrected second query. The realm is reset and the
uncertainty recorded. No retry or continuation is allowed.

## Review and live prerequisites

Before any live send, committed candidate bytes require a direct-byte review
package, independent Sol High review with zero findings, a later
non-self-referential classification commit on that PASS parent, and a separate
post-commit coordinator tuple.

Action time additionally requires all candidate/runtime/documentation hashes,
the clean evidence worktree, public DNS absence, zero local residue, VM1205 safe
checkpoint, sole live-resource lane, current task identity, exact 12-path dirty
baseline, stable projection, fresh realm, direct declaration audit, and new
external Chrome profile/window/tab and non-conflict confirmation.

The final Create/native Copy/native masked Paste confirmation remains external,
mandatory, action-time, and unreached. The later exact-row deletion confirmation
remains separate, mandatory, and unreached.
