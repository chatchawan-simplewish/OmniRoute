# V61 replacement receiver-launch contract

Status: PROPOSED; independent Sol High PASS and fresh action-time evidence are
required before consumption. `authorizes_live_execution=false` for this static
artifact. Standing owner authority supplies execution authority only once those
conditions pass. V60 remains closed; no predecessor gate is reused.

## Ownership and fixed scope

Sole Sol High owner: `/root/v60_receiver_gate_owner`. Sole worktree:
`C:\ChatGPT Projects\SW-Selfhosted-Network\.worktrees\omniroute-agent-routing-source`.
Base HEAD: `eeb2bb690c6b86bf3a58686c65fcd82c42588e00`. The only permitted
descendants before consumption are this V61 package and its review/evidence;
index must be empty and the 12 existing product dirty entries preserved.
Their exact paths/hashes are in
`task-2-secure-console-transfer-v61-product-baseline.json`; verify each at
action time. `test-v61-static.ps1` is the non-evaluating parser/pin/guard-order
and product-hash check, run from the source-worktree root using the pinned
runtime. It starts no receiver, proxy, browser, clipboard, R5, or network work.

One new private `team-api-proxy` start/proof, one exact script preparation,
one retained visible masked-input receiver, then pause before final Create.
No Cloudflare Create, Copy, Paste, token use, R5 execution, revocation, public
rollout, VM power change, model request, permission/routing change, binary
installation/substitution, secret inspection, or alternate transport is granted
by V61. V59 and the original separately reviewed post-accept contract retain
their mandatory action-time confirmations. Do not ask for or accept a token
until that later authority is separately satisfied.

## Authoritative sources and exact derivation

The original live brief at baseline is 62054 bytes, SHA-256
`995CA4D59E564EAF53417B9F577808F9C307C5CDEE93FB8E91DC73A5211F52CC`.
The original scope-replacement brief is 41236 bytes, SHA-256
`44D690E12123B29745442934A5C32C11B6914978768A974735C7404B87630962`.
Its fix4 Sol High review remains the inherited cleanup reference. These old
files stay unchanged and are not executable authority. All paths below are
in this file's directory.

| New source container | Bytes | SHA-256 |
| --- | ---: | --- |
| `task-2-secure-console-transfer-v61-live-source.md` | 38868 | `BF377E54C0DA83BF2A182348E4FE5C4F0CB409617D68AE329945555B036AFDD2` |
| `task-2-secure-console-transfer-v61-scope-source.md` | 20509 | `062B4EC650BF7BEA2F951B9DD4FFD42D4D558A0901C9D6DD5B62DC125A7AC403` |

Only these changes to original executable fences are allowed:

1. Runtime SHA/version advance to the observed Microsoft-signed bundled binary.
2. Add the same path/length/hash/version guard before owner readiness, retaining
   the existing immediately-before-R5 guard; add a parent prelaunch guard.
3. Update owner byte/hash constants and every preparation/launch extraction
   path, length, and hash transitively to the new source containers.
4. Make the receiver's interactive visibility explicit with `-WindowStyle
   Normal`. This is the user-controlled masked input window, not a background
   helper. No native UI keystroke injection is permitted.

The private proxy wrapper, deterministic R5 script, final owner cleanup fence,
and post-launch pre-accept disposition fence are byte-identical to their
reviewed originals. The fixed temp direct-child guards, same-parent dot-source
semantics, no-reacquisition matrix, timeout budgets, counter tuples, credential
redaction, revocation hold, and cleanup conditions are not relaxed.

## Transitive executable pins

All fences are strict UTF-8, LF-only, with one trailing LF. Index is zero-based
among `powershell` fences in the named container. Extraction and parsing are
allowed during review; evaluation is forbidden.

| Container / index | Role | Bytes | SHA-256 |
| --- | --- | ---: | --- |
| live / 0 | proxy start and 19-label proof | 8380 | `A7C7F04705344160030C3BF1CA383179616507555F56D2288E3BFD9B46FEF2AC` |
| live / 1 | exact preparation, used only through scope / 0 | 3988 | `83440D05C9B2486509D2DC9A51F10C4916FFAFFC1C6B7B6FABCE07188FAE76D5` |
| live / 2 | owner | 22582 | `383E21D339AD67D704F1515F2091D1363809CC23DC61176EE65246012A5E476A` |
| live / 3 | launch, used only through scope / 1 | 675 | `C4DDD2E84C14C125830DD422C613CE9DC4CFCDBCABB760F003B9187811B7D2FA` |
| live / 4 | final owner cleanup | 2779 | `9316DE2F7C84A89947E57C8563E9381E97D7C52F0DFFE56CA7E4DE616DD8F637` |
| scope / 0 | retained preparation | 2022 | `C34931235DB0BC7AD5CC441D9C6CD1BEF27DA436C902F21023FE2682881F3AA8` |
| scope / 1 | guarded retained launch and cleanup functions | 14846 | `80DC8090F28FE6FB949616F8295C1CE14F637E049C69750B5ED8F83EAF83CFF5` |
| scope / 2 | pre-accept failure disposition | 3308 | `B8087F2CB77A695C1B6DD145DDD3D39D48F8AABEBBA00863BEA0610C8BE6518E` |

R5 source container `task-2-rulesets-api-incident-sol-review.md`: 32520 bytes,
SHA-256 `1950DC72285AA349E33CDFEDCE948C7A99CADF1F70528DF22F643FBA07FBB33A`.
Its deterministic R5 fence: 10890 bytes, SHA-256
`DB75253CD851075C1D612A54EC4B02C8016C034C8BC192A3DB9D02DB9890AD41`.
V61 may prepare but must not start that child without separate later authority.

Bundled runtime:
`C:\Users\chatc\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\powershell\pwsh.exe`;
301368 bytes; version `7.6.5.500`; SHA-256
`362A356CE7F0940EC74F73A8FC2C990A2CC24A38A11C90BBD8ECA947110AD139`.
Authenticode must be Valid, signer subject exactly
`CN=Microsoft Corporation, O=Microsoft Corporation, L=Redmond, S=Washington, C=US`.

## Fresh browser binding, not an inherited alias

Preparation observation in the current CUA session, 20260904 123149 Bangkok:
browser `1`, family Chrome, extension instance
`dfd27eec-84b9-446f-81ec-a134992060a6`, profile
`Codex-Chrome-Bell-PC2`; sole listed tab `435370534`; provider identity
`["31166783-726d-44e2-afb8-597b00fb6f06","435370534"]`.
The sole source/receiver owner freshly selected this exact tab as `v61Tab`.
No old browser ID `2`, old VM, old prototype, old JS gate, or old alias is used.

Observed exact URL:
`https://dash.cloudflare.com/profile/api-tokens?search=OmniRoute+secure+console+R5+20260901`.
The visible summary contains the fixed token name, `Digital@simplewish.me's
Account`, one `mysw.me` zone, `Zone WAF:Edit, Zone:Read`, and an unactivated
Create Token control. This is preparation evidence, not fresh token-absence
proof and not a consuming-gate PASS.

Immediately before action-time provider checks, use one supported
`cua.getState({emit:false})` and privately match those exact profile, extension,
browser, tab, provider, and URL pins. Emit only match booleans and counts.
Require exactly one matching browser and one matching tab, no competing lane,
and retained `v61Tab.id` equal to the fixed tab ID. If absent/stale/mismatched,
stop; no rebind/reconnect/alternate tab. Do not inspect another task's tab.

Use only the retained `v61Tab` and its documented CUA/Playwright interface for
serial credential-free provider checks. AX/DOM observation is limited to
non-secret form, table-name/count, and resource-scope facts. No token value,
clipboard API, generated-token page, arbitrary browser endpoint, VM bridge,
or page export is permitted. No browser calls at all after final Create.
Navigation for credential-free checks is permitted only within this assigned
tab; return to the exact V59 summary and re-prove its scope before launch.
The original retained-tab adoption/detachment JS fences do not run in V61.

## Ordered action-time preconditions

Complete all before any clipboard clear, proxy start, preparation, or receiver
launch. Record fixed status/count/hash evidence only. Missing evidence is not
a PASS. Any failed/ambiguous check closes V61 before later action, without retry.

1. Empty index; exact base ancestry with V61-only descendant changes; same 12
   dirty product paths and their recorded pre-review hashes. Verify every
   source/fence/R5/runtime pin and parse all eight PowerShell fences with zero
   errors. No old runtime hash or old live/scope extraction path may remain in
   either new executable source container.
2. Windows CIM `Microsoft Windows 11 Pro`, version/build `10.0.26200 / 26200`,
   `64-bit`; registry `Windows 10 Pro`, `Professional`, `25H2`, UBR `9278`.
   Both `AllowClipboardHistory` and `AllowCrossDeviceClipboard` present as
   `DWord 0`. Do not modify policy or claim clipboard-history erasure.
3. Fresh browser/controller checks above; authenticated Cloudflare account
   `digital@simplewish.me`; fixed token exact-name and matching-row counts `0/0`.
   Exact summary: name `OmniRoute secure console R5 20260901`, only `mysw.me`,
   only Zone WAF Edit and Zone Read. No additional zone/account permission.
4. Inherit all original live prerequisites: VM1205 `omniroute` and
   `bell-cloudflare-proxy` running; target proxy/team Tunnel absent; network
   `omniroute-internal` has 2 members; listener `20130` absent; Caddy SHA-256
   `A31C2010BB47767E25A826CEBCD2469CB51D8AF5C5AE089E46D2051EDD69D9BB`;
   target DNS/Tunnel/connector/overlapping Access absent; one unrelated
   `http_ratelimit` rule and zero target description/expression matches;
   OmniRoute `team-rollout-test` absent; public resolvers `1.1.1.1` and
   `8.8.8.8` target A/CNAME counts `0/0`; no current prepared temp candidate
   or retained credential owner from a predecessor. No process enumeration,
   name/PID reacquisition, old v3/v5 inspection, or cleanup is authorized to
   manufacture that proof. If current safe absence cannot be established,
   stop before launch and report it NOT PROVEN.

## Retained parent and one-shot launch sequence

After review PASS, start exactly one credential-free persistent parent using
the pinned runtime through `exec_command`, `tty:true`, source-worktree cwd:

```powershell
& 'C:\Users\chatc\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\powershell\pwsh.exe' -NoLogo -NoProfile -NoExit
```

Retain its returned session ID and use only `write_stdin` on that exact session.
Do not start another parent. A failed/uncertain start or lost session closes
V61; no attempt to recover variables, PIDs, or paths elsewhere. This parent
contains no token, header, or Zone ID. Before live actions its exact runtime
path/hash/version and working directory must match the pins. Read-only shell
commands run earlier for drafting are not this persistent parent.

After every prerequisite passes, consume V61 once: clear the current Windows
clipboard once with `Set-Clipboard -Value ''`, then read once shape-only with
`Get-Clipboard -Raw`, discard the value, and require empty TRUE with both
attempted/fulfilled counts `1/1`. Never emit clipboard bytes.

In that same retained parent, extract and verify all source/fence pins before
evaluating any. Then dot-source, in order, exactly once each: live / 0, scope /
0, scope / 1. Never separately execute live / 1 or live / 3. Require exact
`PROXY_START_AND_PROOF=PASS`, `EXACT_RETAINED_SCOPE_PREPARATION=PASS`, and
`EXACT_RETAINED_SCOPE_OWNER_LAUNCHED=PASS START=1/1 HANDLE=TRUE`.
Retain the exact `$owner`, `$expectedOwnerPid`, `$ownerClock`, prepared paths,
and preloaded disposition in that same parent. Do not return ownership while
the receiver is live. All raw child output remains private.

Observe only nonblocking `$owner.HasExited` and the exact safe log for up to
30 seconds after launch. Require exactly one retained-PID match and the two
exact readiness markers, with no malformed or unrecognized output. Incomplete
startup output may be observed again within this same deadline; this is not
another launch or gate attempt. Do not wait on the process while coordinating.
Readiness failure, timeout, output uncertainty, or receiver exit closes V61
and immediately invokes the single pre-accept disposition in the same parent.

On readiness PASS, report only safe PID/readiness evidence and pause before
final Create. The sole owner remains active and retains the parent. Mandatory
combined final-Create/native-Copy/masked-Paste confirmation and later separate
exact-row deletion confirmation remain unsupplied. No automated keystrokes,
secret reads, transfer, R5, or provider mutation may be inferred from readiness.

## Failure, cleanup, and review acceptance

All inherited no-retry and bounded-cleanup branches remain controlling. Before
start, remove only exact new, hash-proven direct-child preparations and the
exact new proxy when the reviewed guard permits. After start, no file/root
deletion before retained-handle exit proof; if start is uncertain without a
handle, preserve all state and stop NOT PROVEN. Never enumerate/reacquire.
Use only the one preloaded scope / 2 disposition; it invokes the unchanged
remaining-budget final block and guarded proxy cleanup. No second wait/kill/
drain/dispose/cleanup, retry, fallback, override, or verdict relaxation.
Long shell waits must yield tool control within 10 seconds so status updates
remain possible; no tool wait above 60 seconds is required.

Independent Sol High review must verify direct bytes, exact derivation and
all transitive pins, non-evaluating parser checks, current-runtime provenance,
new CUA binding eligibility, action-time preconditions, retained parent scope,
visible receiver, same-process readiness, full failure/cleanup matrix, and
separation from V59/post-accept authority. Any finding or missing proof blocks
launch. Record review and execution separately; static PASS is not readiness.
