# Existing caller authentication metadata read

Scope: one direct GET `/v1/models` on the same VM1205 HTTP origin already configured for `omniroute-admin`, using that integration's existing bearer environment variable `OMNIROUTE_ADMIN_TOKEN`. Local metadata verified the exact origin/path, this non-secret variable name and env presence, with no values emitted. This contract explicitly authorizes this cross-endpoint metadata diagnostic under the user's in-scope implementation/diagnosis authority after independent review. It does not expand persisted credential permissions. No new credential, key rotation, generation, provider probe, configuration write, MCP session mutation or old gate execution.

Exact script: `scripts/auto-switch-auth-read-20260912.py`. It parses only the existing local Codex configuration in memory, requires origin `http://192.168.1.68:20128`, MCP path `/api/mcp/stream`, no URL userinfo/query/fragment, and exact credential reference `OMNIROUTE_ADMIN_TOKEN`, then sends that existing bearer only to `/v1/models` at the fixed same origin. It uses Python http.client directly (no proxy environment or redirects), a 10-second socket timeout, no retry, and bounded response bytes. It prints only HTTP status, model count, and two alias-presence booleans. All exceptions are reduced to class name; no secret hashes, headers, raw bodies or URLs with credentials. Existing ordinary server request/audit counters may update as a consequence of GET; no deliberate DB/config mutation is performed.

Existing secret-use authority is restricted to its intended provider origin. There is no credential transmission to another service or storage destination. This does not create a harness credential or authorize reusing the admin key in client configuration.

Execute with the currently resolved local Python interpreter after independent Sol High review and immediate script/contract SHA-256 checks. Command from coordinator worktree: `python scripts/auto-switch-auth-read-20260912.py`. One GET only. Stop on mismatched configuration, missing/malformed token, connection failure, redirect or non-200 status. Do not repair credentials or retry by implication. Evidence consists of sanitized stdout/exit status. No rollback is required for this metadata request.

A 200 result proves the existing token can authenticate model discovery directly; it does not prove inference or explain why the older MCP generation probes failed. A 401 result establishes current direct authentication failure only. Neither outcome authorizes relaxing auth or running generation.
