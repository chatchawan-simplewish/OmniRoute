# Candidate-only Codex deactivation R1 contract — 2026-09-13

**Status: PREPARED SOURCE / NOT AUTHORIZED.** The launcher's reviewed entry point may be used only after a fresh independent Sol High review, exact reviewed-pins artifact, action-time pin and result-absence validation, and explicit sole-owner dispatch. It is a new candidate-only mutation; it does not retry any spent key or preflight gate.

The target is the exact running, healthy, network-none R4 candidate `9468859edcdb483c53900edde14d301a162cccc791079154677e913f39bf26a3`, image `sha256:8211e1071a3b68eac01673d76129150eb0c0fea329dd222bc8bf0394b13fc844`, source `e53d895e9a5e38a7f06ce59de254835f10e829c1`, private volume `omniroute-auto-switch-candidate-data-r4-20260913`, unchanged live container/volume, and `cloudEnabled=false`. The exact Codex row is `8f92f200-d280-47b3-b380-2d94be0a4b75` with provider `codex`.

The native candidate loopback operation performs one authenticated GET first. It requires the exact sanitized row and boolean `isActive`, with access, refresh, and ID token fields absent. If already inactive, it skips mutation. If active, it sends exactly one `PATCH /api/providers/{id}` with only `{"isActive":false}`, requires HTTP 200 and the exact sanitized inactive row, then performs one final GET and requires the same inactive readback. No credential value is read or emitted. The accepted native update may perform its normal candidate-local transaction, database backup, cache/config-generation invalidation, audit record, and `updated_at` write; `cloudEnabled=false` makes cloud sync return before network use.

The management token remains only in local process memory and SSH/Docker-exec stdin. It is removed from the inherited SSH environment, never placed in argv, Docker environment, result, or logs, and all child output is captured privately with fixed bounds. The result is reserved before SSH with `O_EXCL`, limited, fsynced, closed, and contains only fixed status/stage, public connection/candidate IDs, booleans, HTTP statuses, and categories.

Before PATCH dispatch, any identity, schema, HTTP, parse, or predicate failure is a terminal `STOP`. After PATCH dispatch, any timeout, overflow, stderr, malformed response, non-200 result, or failed final readback is terminal `UNKNOWN` with the known candidate/connection ID and stage. There is no retry, reactivation, SQLite edit, restart, cleanup, fallback, provider request, model call, or compensation. A PASS proves only that this isolated candidate's Codex connection is inactive; a fresh independently reviewed all-17 read-only preflight and a separate reviewed key-creation successor remain required.
