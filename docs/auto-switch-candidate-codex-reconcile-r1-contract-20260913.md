# Candidate Codex reconciliation R1 contract — 2026-09-13

**Status: PREPARED SOURCE / NOT AUTHORIZED.** This new read-only reconciliation requires a fresh independent Sol High source review, exact reviewed-pins artifact, action-time pin and result-absence validation, and explicit sole-owner dispatch. It does not retry the spent deactivation gate and does not infer that its unknown PATCH did or did not commit.

The target is the exact R4 candidate `9468859edcdb483c53900edde14d301a162cccc791079154677e913f39bf26a3`, image `sha256:8211e1071a3b68eac01673d76129150eb0c0fea329dd222bc8bf0394b13fc844`, source `e53d895e9a5e38a7f06ce59de254835f10e829c1`, tag `omniroute-auto-switch-r3:e53d895e9a5e38a7f06ce59de254835f10e829c1`, and volume `omniroute-auto-switch-candidate-data-r4-20260913`. The remote program uses exact-name formatted Docker metadata only, repeats candidate inspection by name and full ID, and requires running/healthy, network-none, zero ports, user `node`, read-only root, restart `no`, exact noexec tmpfs, all capabilities dropped, `no-new-privileges`, exact volume mount, exact image identity, unchanged live container, and all three exact stopped retained candidates.

Only after those checks pass, it sends one Node program through `docker exec -i` stdin to the already-running candidate. That program performs exactly one authenticated `GET /api/providers/8f92f200-d280-47b3-b380-2d94be0a4b75`, requires HTTP 200 and the exact sanitized `codex` row, rejects access, refresh, ID-token, or API-key fields, and emits only the public ID/provider plus boolean `isActive`. It performs no PATCH, POST, SQLite or file access, provider/model call, start, stop, restart, cleanup, retry, or configuration change.

The existing admin token remains only in local process memory and nested SSH/Docker stdin. It is removed from the inherited SSH environment and never appears in argv, Docker environment, result, test fixture, or logs. All child output is captured privately with fixed bounds. The local receipt is reserved before SSH with `O_EXCL`, size bounded, fsynced, closed, and includes only fixed status/stage/categories, sanitized remote fields, and bounded child return-code/output byte-count/hash evidence. It retains no raw stdout, stderr, response body, environment, or credential value.

A valid GET yields `PASS` with the exact candidate and connection identities and observed `isActive` boolean. A validated HTTP/schema/parse failure yields `STOP`. Timeout, overflow, stderr, malformed or contradictory child output, or transport uncertainty yields `UNKNOWN` without inferring the Codex state. Any invocation is spent and has no retry or fallback. PASS proves only current Docker/isolation metadata and the one native Codex-row observation; it does not prove database/native health, startup history, key readiness, hardware qualification, or rollout readiness.

Preparation verification is limited to the focused whole-rendered fake-Docker/HTTP test, launcher self-check, Python compilation, and diff checking. These checks perform no SSH, Docker, HTTP, credential, provider, or live-resource action.
