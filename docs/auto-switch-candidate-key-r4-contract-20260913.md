# Native R4 candidate qualification key — source contract — 2026-09-13

**Status: PREPARATION ONLY / NOT AUTHORIZED FOR EXECUTION.** Source binding is complete, but no live key operation is authorized until this exact revision receives independent Sol High review, exact reviewed pins, result absence, action-time revalidation, and explicit sole-owner dispatch. The spent R4 startup receipt remains exact `UNKNOWN` SHA-256 `cbbd08cfd678021d67bc6f8a799743cfd00d72cff1ce0bbc73960d945e1348bf`. Reconciliation R1 remains exact `STOP` at `candidate_discovery`, SHA-256 `6d7a9c135dfd16375de82a2497444fde72cd869795cf3ee092d6c4d4075aac50`. Neither is reinterpreted or retried.

The accepted read-only R2 reconciliation receipt is pinned at SHA-256 `7efe1cb83cc1c5c6cf85199b919338bb2a6143054d9a6cd315e0c3270fc213cf`. It identifies candidate `9468859edcdb483c53900edde14d301a162cccc791079154677e913f39bf26a3` as running, Docker-healthy, and isolated. Its explicit limits remain controlling: environment, database, and native health were not checked and historical startup was not reconstructed.

The only target is the reconciled running and healthy `omniroute-auto-switch-candidate-r4-20260913`, image `sha256:8211e1071a3b68eac01673d76129150eb0c0fea329dd222bc8bf0394b13fc844`, source `e53d895e9a5e38a7f06ce59de254835f10e829c1`, volume `omniroute-auto-switch-candidate-data-r4-20260913`, network `none`, read-only root, exact hardened `/tmp`, no published ports, no restart policy, all capabilities dropped, and only `no-new-privileges`. Action-time checks repeat the candidate by name and exact ID, image/tag, sole exact local volume, `cloudEnabled=false`, migrations `170_agent_route_runs` and `171_agent_route_deferred_metrics`, exact agent-route table columns including nullable `deferred_requests`, and unchanged live container identity. Native management GETs must also prove the key API schema, combo API schema, and inactive Codex connection before creation. The launcher never starts, stops, restarts, removes, reconnects, or alters a container, volume, live service, network, gateway, DNS, provider, client, or routing default.

The future gate creates only `auto-switch-candidate-qualification-r4-20260913`. Before creation, native management GETs require that name and the nonmatching combo sentinel absent and the Codex connection inactive without accepting token fields. Through the candidate loopback API, perform exactly one POST and one exhaustive permission PATCH, then one exhaustive GET. Require the original 14 ordered models, five ordered connections, `agent:route`, endpoints `chat` and `models`, restricted model access, a nonmatching combo sentinel, no quotas, `noLog=true`, `autoResolve=false`, active/unbanned lifecycle, and the accepted native defaults. Query only `id,ip_allowlist` from the read-only SQLite database and require SQL NULL. No model, catalog, provider generation, OAuth, or egress request is made.

The management token exists only in launcher memory and the reviewed SSH/stdin payload. It is removed from the child SSH environment, never placed in argv, Docker environment, files, output, or evidence, and never printed. The new key secret remains inside the remote Python/Node pipeline. Only after exhaustive readback may it be written to `/root/.omniroute-qualification/auto-switch-candidate-qualification-r4-20260913.key` under the existing exact root-owned non-symlink `0700` parent using `O_EXCL|O_NOFOLLOW`, root ownership, mode `0600`, and fsync. No secret value, hash, mask, length, response body, header, database row, or exception text crosses SSH.

If a validated authoritative native failure occurs after the new key ID is known, the same bounded remote process makes at most one compensating PATCH to deactivate that exact ID and one GET to prove inactive. A native rollback is never repeated by the enclosing process. Any store created by this invocation is removed only after stable inode verification through the held parent descriptor, then the parent is fsynced. Creation likewise uses that held descriptor and fsyncs the leaf and parent. A child timeout, output overflow, stderr, malformed envelope, or otherwise unvalidated response after native dispatch is terminal `UNKNOWN`: candidate, key, store, and rollback fields remain null, and there is no retry or cleanup. If POST may have committed without returning an authoritative ID, a separately reviewed name-only reconciliation is required. Every invocation is spent. A PASS receipt additionally records both required migrations as true; it does not establish provider inference or hardware behavior.

Reserve `docs/auto-switch-candidate-key-r4-result-20260913.md` with exclusive creation before reading the management token or opening SSH, and fsync one bounded secret-free PASS, STOP, or UNKNOWN receipt. Preparation checks are limited to the focused local test, launcher `--self-check`, in-memory Python compilation, and diff checking. The focused test performs one disposable native POST/PATCH/GET plus SQL-null check against a temporary database in the accepted source worktree. It also executes the complete rendered remote Python path with deterministic fake Docker metadata through identity, server.env, credential aggregate, schema, native result, protected store, final inspection, and genuine outer validation. Separate simulated post-create timeout, overflow, and malformed-envelope cases require `UNKNOWN`, all mutation-state fields null, no store, and no retry despite a committed fixture row. These checks use no SSH, Docker daemon, remote service, real credential, provider, or live resource. Passing them proves source preparation only and does not satisfy the hardware qualification prerequisite.
