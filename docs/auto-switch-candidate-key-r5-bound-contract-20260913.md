# Candidate key R5 bound execution contract — 20260913

**SOURCE ONLY — NOT EXECUTABLE until independent Sol High review freezes this launcher, focused test, contract, reviewed payload, and all dependency hashes.**

This is the minimum execution binding around the unchanged and never-invoked R5 key implementation. It pins the original R5 launcher, focused full-emitter test, contract, historical candidate receipts and transport dependencies; the frozen R4 ordered-preflight launcher, test, contract, independent review and reviewed pins; the exact accepted R4 receipt SHA-256 `8cdae537781844cc91174f810b2c5933ce1bd1c53a0fb175831a7cfaf6ca3b61`; and its evidence review. The R4 receipt must validate through its frozen validator as all 17 predicates true for exact candidate `9468859edcdb483c53900edde14d301a162cccc791079154677e913f39bf26a3` before any credential access or dispatch.

The native mutation remains exactly the R5 implementation: create the fresh unique key name `auto-switch-candidate-qualification-r5-20260913`, PATCH and exhaustively read back the original ordered 14 models, five connections, sentinel-only no-combo access, no quotas, scope `agent:route`, endpoints `chat` and `models`, `noLog=true`, `autoResolve=false`, SQL-null IP allowlist and inactive Codex, then publish the secret through the held root-owned directory descriptor to the exact `0600` operator store. Existing candidate, image, volume, environment, credential aggregate, schema/migrations, current key/store absence and live-service identity are revalidated by the unchanged R5 remote program immediately before mutation.

The management token stays in process memory and SSH/Docker-exec stdin only and is removed from the SSH child environment. The generated key stays within the remote native child and protected store. Neither secret enters argv, stdout, stderr, logs, reviewed pins, or the result. The launcher preserves the R5 conditional exact-key rollback after authoritative failures. Timeout, output overflow, stderr, malformed or contradictory native result, or post-dispatch ambiguity records UNKNOWN without retry or additional cleanup.

The fresh local result `docs/auto-switch-candidate-key-r5-bound-result-20260913.json` is absent before dispatch, reserved with `O_EXCL`, bounded, canonical, fsynced and closed. The wrapper records only the validated sanitized R5 result plus return code, byte counts, hashes and fixed category. Any invocation is spent. No provider/model generation, container lifecycle, live-service, client, gateway, DNS or unrelated key action is in scope.

No additional manual native confirmation is required by this source. Execution still requires independent Sol High PASS review, exact reviewed pins, action-time revalidation, and explicit sole-owner dispatch under the project authority contract.
