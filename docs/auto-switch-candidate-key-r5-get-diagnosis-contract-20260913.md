# Candidate key R5 native GET diagnosis contract — 2026-09-13

**Status: PREPARED SOURCE ONLY; NO EXECUTION AUTHORITY.** This fresh read-only observation explains the spent R5 preflight's unresolved `native_preflight` STOP. It is not a retry of candidate-key R4, candidate-key R5 preflight, or any key-creation gate. Independent Sol High review, exact reviewed pins, result absence, and an explicit sole-owner dispatch are required before invocation.

The launcher pins candidate `9468859edcdb483c53900edde14d301a162cccc791079154677e913f39bf26a3`, frozen candidate-key R5 source `191913463849c7c42ae2e52126f73825b21bc661b6e88bfe6a799ed656f19273`, and bounded strict-SSH helper `78bb97e57d6229aae84c2f0d0caaf68eeb71ba6b92e69415d83af2a869b8224a`. It renders the exact R5 read-only candidate, live, image, volume, SQLite, `server.env`, store-absence, and hardening preconditions, then replaces only R5's native preflight branch. All preconditions must pass before the native child runs.

The native child performs exactly one `GET /api/keys` at candidate loopback `127.0.0.1:20128`. It follows no redirect, has an eight-second deadline and 1 MiB body cap, and makes no POST, PATCH, provider, inference, models, combo, or Codex request. The existing management token stays in local memory and SSH stdin, is removed from the SSH child environment, and is never written, printed, hashed, returned, or sent to another origin.

The remote result contains only the candidate ID, precondition boolean, bounded HTTP status/category, JSON-parse boolean, one fixed schema category, `keys_is_array`, R5-key-name-presence, and one fixed child-outcome category. Completed JSON observations require a present status with its exactly derived category and internally consistent object/array/name-presence fields. An HTTP response with invalid JSON retains its safe integer status/category while all shape fields remain null/unparsed. Timeout, connection, and output-limit outcomes require null status, category `none`, and null/unparsed shape fields. It never contains a response body, response field names outside the fixed classifications, key rows, key IDs, masked keys, counts, headers, credentials, or arbitrary error text. A precondition failure may return the frozen R5 preflight STOP booleans. Local transport ambiguity writes only terminal `UNKNOWN`.

The fixed result is `docs/auto-switch-candidate-key-r5-get-diagnosis-result-20260913.json`, reserved with `O_EXCL`, bounded, canonically encoded, fsynced, and closed. No retry, fallback, cleanup, state change, key/store operation, candidate restart, provider call, or generation follows any outcome. Focused verification is limited to the test, launcher self-check, Python compilation, and exact diff review; these run no SSH, Docker, credential, API, or network action.
