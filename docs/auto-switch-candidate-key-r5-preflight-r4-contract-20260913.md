# Candidate key R5 preflight R4 contract — 20260913

**SOURCE ONLY — NOT EXECUTABLE until independent Sol High review freezes the exact launcher, test, contract, dependencies, and reviewed payload.**

R4 is a fresh receipt-owning wrapper around the frozen ordered R3 renderer and validator. It does not invoke or alter the spent R3 entry point or receipt. It pins the R3 launcher, test, contract, spent receipt, corrected evidence review, transport helper, all transitive R3 route and runtime hashes through the exact nested R3 review payload, and the accepted current Codex reconciliation launcher, test, contract, review, reviewed pins, receipt, and evidence review.

Before credential access or dispatch it requires all pinned regular no-symlink files, exact reviewed-payload equality, normal `PROGRAMDATA`, absent `PYTHONOPTIMIZE`, the fixed fresh result path, and the exact accepted reconciliation receipt. That receipt must validate as candidate `9468859edcdb483c53900edde14d301a162cccc791079154677e913f39bf26a3` running, healthy, network-none and isolated with unchanged live/retained identities, and exact Codex connection `8f92f200-d280-47b3-b380-2d94be0a4b75` inactive after HTTP 200.

The one reviewed invocation may perform only the frozen ordered calls: one `GET /api/keys`, one `GET /api/combos`, and one `GET /api/providers/8f92f200-d280-47b3-b380-2d94be0a4b75`. It preserves the accepted 17 predicates, exact per-step status/schema/category evidence, true-prefix STOP semantics, and PASS only when all 17 predicates pass. It performs no POST, PATCH, key/store creation, SQLite write, container lifecycle operation, provider/model request, retry, fallback, cleanup, credential rotation, or secret output.

The management token remains in local process memory and remote stdin only, is removed from the SSH child environment, and is never written to argv, stdout, stderr, or the receipt. SSH stdout/stderr remain private and bounded by the frozen transport helper. The result records only a validated sanitized remote envelope plus return code, byte counts, hashes, and fixed category. Stderr, timeout, overflow, malformed JSON, invalid envelope, or status contradiction produces UNKNOWN with no remote projection or retry.

The launcher reserves `docs/auto-switch-candidate-key-r5-preflight-result-r4-20260913.json` with `O_EXCL`, writes one bounded canonical PASS/STOP/UNKNOWN receipt, fsyncs, and closes it. Any invocation is spent. A PASS establishes only the named read-only predicates at that observation time; it does not create a key or authorize hardware traffic.
