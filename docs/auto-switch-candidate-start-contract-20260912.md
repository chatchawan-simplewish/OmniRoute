# Offline candidate clone/start contract — 2026-09-12

**Status: PREPARATION ONLY; NOT EXECUTABLE.** The launcher and this contract require fresh independent Sol High review and exact reviewed pins before one candidate-start invocation. The accepted R4 transfer result is now bound as a prerequisite, but these updated preparation bytes do not themselves authorize any clone, volume, container, secret, service, provider, client, or live operation.

## Fixed target and prerequisite

Target only VM1205 `belladmin@192.168.1.68` using the frozen transport helper. The live service must retain container ID `7b20ca195e3c9e875d0a1ce98832ca469c2a114886335b8466b1467b3ed139bc`, image `sha256:60ab56311d1c9873416dc53683148d314465370312547b8ce87c442ce084c6a5`, running state, and sole `/app/data` volume `omniroute-data-mcp-audit-20260910-r3`. The candidate uses source `dc53bcfed67b3bbea7d2fbf82468342e573dadb3`, tag `omniroute-auto-switch-r2:dc53bcfed67b3bbea7d2fbf82468342e573dadb3`, and immutable IID `sha256:a91994bf883698d4520ff048d16be999d4614331a528c76c0b95bc6dc8bec803` with Linux/amd64, user `node`, and exact revision label.

Accepted result `docs/auto-switch-image-transfer-result-r4-20260912.md`, SHA-256 `4b0eda575ecf465d184c654e95ed5eee58e2ebfa4696797236bba3e5d2f05736`, with exact status `IMAGE_TRANSFER_R4_PASS` is a hard prerequisite. The launcher binds that literal path, hash, status, source commit, candidate tag, and immutable IID; the future reviewer must also pin the result SHA-256 together with the launcher, contract, binding file, and frozen transport helper. The launcher revalidates the exact tag/IID/platform/user/revision on VM1205. A missing, changed, malformed, stale, or unreviewed transfer result stops before SSH or mutation.

## Clone and secret boundary

Candidate container name is `omniroute-auto-switch-candidate-20260912`; its new private volume is `omniroute-auto-switch-candidate-data-20260912`. Both must be absent before mutation. The candidate never mounts the live volume. Before any source secret or SQLite read, a root-only host process requires `docker volume inspect` to return exactly the pinned live volume name with built-in driver `local` and scope `local`, then requires its mountpoint to be a canonical regular directory. It uses Python SQLite's backup API from a read-only source connection to create `storage.sqlite` in the new volume. This is the only database copy: no database/WAL/SHM byte copy and no candidate or helper container mounts the live volume. The new volume directory is UID/GID 1000 and mode `0700`; its clone database is UID/GID 1000 and mode `0600`.

Read only the canonical live-volume `/app/data/server.env` equivalent with a bounded regular, canonical, non-symlink, no-follow, stable-file read. Require configured `STORAGE_ENCRYPTION_KEY` to remain absent from the live Docker environment. Preserve only its final nonempty `STORAGE_ENCRYPTION_KEY` assignment and optional nonempty `STORAGE_ENCRYPTION_KEY_VERSION`. Preserve `JWT_SECRET` and `API_KEY_SECRET` only from their exact nonempty live Docker configured-environment entries. Do not preserve `INITIAL_PASSWORD`, whole environment data, the whole live `server.env`, or any other secret. Values remain only in protected remote-process memory until written by exclusive no-follow creation to the candidate volume's `server.env`, then fsync, chown to UID/GID 1000, and verify mode `0600`. Secret values never enter command arguments, Docker environment flags, Windows, stdout/stderr, evidence, hashes, or logs.

Use the native SQLite clone only. Require source and clone `PRAGMA quick_check=ok`, native credential columns, and the observed `18` encrypted, `0` plaintext, `34` empty credential-field counters before start. On the clone alone, insert or replace `key_value(namespace='settings', key='cloudEnabled')` with the JSON literal `false`, commit it, and read it back as exactly one JSON boolean false row before creating the candidate. Never modify the live database or volume.

## Candidate isolation

Create the candidate with its new volume mounted read-write only at `/app/data`, configured user `node`, `--network none`, no published ports or other network, read-only root filesystem, `/tmp` tmpfs limited to 64 MiB with `rw,noexec,nosuid,nodev,mode=1777`, all capabilities dropped, `no-new-privileges`, and restart policy `no`. Mount no host path, live volume, Docker socket, credential directory, or other volume.

Pass only the nonsecret candidate binding JSON plus this exact background-disable set: `DATA_DIR=/app/data`, `OMNIROUTE_AGENT_ROUTE_BINDINGS_JSON`, `OMNIROUTE_DISABLE_BACKGROUND_SERVICES=1`, `OMNIROUTE_DISABLE_CREDENTIAL_HEALTH_CHECK=1`, `PROXY_HEALTH_ENABLED=false`, `FREE_PROXY_AUTO_SYNC_ENABLED=false`, `OMNIROUTE_ENABLE_LIVE_WS=0`, `OMNIROUTE_DB_HEALTHCHECK_INTERVAL_MS=0`, and `OMNIROUTE_WAL_TRUNCATE_INTERVAL_MS=0`. Docker receives every value explicitly; no host environment is inherited. The immutable image environment may remain only after rejecting storage/JWT/API secrets, `INITIAL_PASSWORD`, cloud URLs/sync secrets, proxy variables, service-discovery variables, management API keys, and provider/client credential prefixes. After create, require the effective fixed values and absence rules again.

Network isolation is mandatory because startup can warm the OpenRouter catalog outside the global background guard. The selected flags suppress the background services, credential health, proxy health/import, live WebSocket, DB health timer, and WAL truncation timer identified by the isolation review. No claim is made that startup is byte-stable: migrations and allowed clone-only startup bookkeeping/cleanup may occur.

## Startup and evidence

Start exactly once and perform a bounded readiness loop only by `docker exec --user node <candidate> node healthcheck.mjs`; this probes the candidate loopback listener inside its `none` network namespace and requires no authentication. Do not read Docker/application logs or use a host/LAN/public port. After health, revalidate the container identity and isolation, clone `quick_check`, exact JSON false cloud state, and unchanged provider-row plus encrypted/plain/empty credential counters. Record only fixed status/stage, immutable container/image/volume identifiers, isolation/health/cloud booleans, database byte and aggregate counters, and schema/key-value counters. Do not output database rows, paths, prompts, responses, headers, cookies, tokens, credentials, secret values/lengths/hashes, environment data, logs, stderr, or provider details.

The native bootstrap decrypt check is nonfatal. Successful startup, health, SQL integrity, and stable credential counters do not prove that the preserved storage key matches ciphertext, that credentials decrypt, or that any provider can generate. This gate creates no API key and makes no management, inference, provider, OAuth, or client request.

## Failure and retention

Every failure reports only `CANDIDATE_START_STOP`, one static stage, and whether a created candidate is confirmed stopped. If a candidate was created and later work fails, issue one bounded stop after revalidating its exact ID and image; never delete it. Retain the stopped candidate and private volume for reviewed diagnosis. Never retry the gate, start, backup, health sequence, or failure action automatically. If stop cannot be confirmed, report false and stop. The live container, live volume, existing services, providers, credentials, routes, and clients remain unchanged.

The runnable offline check is `python scripts/auto-switch-candidate-start-20260912.py --self-check`. It parses the rendered remote payload, verifies the binding hash, and confirms the absence of automatic container/volume deletion; it performs no SSH or live action.
