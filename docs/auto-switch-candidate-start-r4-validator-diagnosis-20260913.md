# Candidate R4 spent-gate local validator diagnosis — 2026-09-13

Status: **LOCAL-ONLY DIAGNOSIS / NO REMOTE ACTION.** The frozen launcher is commit `42c6c99ab17f59c32703e6846f0ec47d22554cd8`, and the immutable terminal receipt is `docs/auto-switch-candidate-start-r4-result-20260913.md` SHA-256 `cbbd08cfd678021d67bc6f8a799743cfd00d72cff1ce0bbc73960d945e1348bf`, recording `UNKNOWN`, `remote_validation`, and `candidate_stopped=null`. The gate is spent and must not be invoked, edited, probed, stopped, or cleaned through that contract.

The remote PASS emitter uses the exact fields `route_runs_before`, `route_runs_after`, `route_turns_before`, `route_turns_after`, `route_events_before`, and `route_events_after`. The local PASS validator instead indexes `name + "before"` and `name + "after"`, producing `route_runsbefore` first. The focused offline reproducer builds the genuine emitted PASS shape and deterministically raises `KeyError('route_runsbefore')`. Replacing only those two concatenations with `name + "_before"` and `name + "_after"` accepts the same shape, proving the terminal `remote_validation` can be fully explained by this local defect without claiming what happened remotely.

The original focused test missed the defect because it rendered and compiled the remote payload and called `offline_check()`, but never passed a successful remote-shaped record to `validate_result`. The emitter field set otherwise matches the validator field set. One additional validation gap is directly reproduced: after correcting the field spelling, a PASS record with `health_probe_attempts=99`, `timeouts=0`, `nonzero=0`, and `successes=1` is accepted. The remote health function normally emits internally consistent counters, so this does not explain the spent failure; a replacement should nevertheless require `attempts == timeouts + nonzero + successes` on PASS as it already does on STOP.

The smallest safe next action is a newly reviewed read-only reconciliation, separate from the spent launcher. It should use the existing strict hash-pinned SSH helper and inspect only the exact candidate name `omniroute-auto-switch-candidate-r4-20260913`, expected image IID/source label, exact volume name, container ID if present, running/status/health/OOM/restart count, network mode, port-publication count, and sole mount identity. Emit fixed booleans and safe IDs only; do not read environment values, secrets, logs, database rows, or arbitrary Docker state. It must not start, stop, create, remove, mount, clean, or retry anything. Only that observation can establish whether R4 is absent, running, stopped, or otherwise unproven before a fresh startup contract is designed.
