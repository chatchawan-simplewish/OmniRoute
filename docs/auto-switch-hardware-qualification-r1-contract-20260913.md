# Actual-hardware qualification R1 source contract — 2026-09-13

**Status: PREPARATION ONLY / NOT EXECUTABLE.** `execution_ready=false`. The accepted reconciliation R2 receipt binds candidate `9468859edcdb483c53900edde14d301a162cccc791079154677e913f39bf26a3`; the later read-only Codex reconciliation proves the exact candidate currently running, healthy, network-none and Codex inactive. The accepted all-17 native preflight and R5 key receipt bind key `a4ba1b25-64cf-4e1b-b694-a1b8bcb94f8a` and its protected root store. Authenticated Q4 catalog readiness has not yet been observed by this gate. This source must receive fresh independent Sol High review, reviewed pins, and explicit sole-owner dispatch before execution. Its integrated pre-mutation checks stop read-only if Q4 authentication, catalog, or any other prerequisite fails. No spent candidate, key, provider, or preflight gate may be reused.

## Bound evidence and current prerequisites

The frozen source/image/tag/name/volume and spent R4 UNKNOWN startup receipt remain exact. Reconciliation R2 proves Docker identity, running/healthy/network-none isolation, and unchanged live/retained containers; the newer Codex reconciliation adds only the exact inactive Codex row at its observation time. The accepted all-17 preflight and R5 key receipts prove their reviewed native snapshots, while this launcher revalidates their exact bytes and the current candidate, environment, database, key, and Q4 state before any mutation. It validates the exact outer and remote R5 key schemas through the frozen R5 validators; the protected store path is bound separately and never required in the sanitized key receipt. Offline fixtures do not substitute for the authentic key PASS.

The bound key must remain candidate-only, active, restricted to the original ordered 14 models and five connections, sentinel-only no-combo access, no quotas, `agent:route`, endpoints `chat` and `models`, `noLog=true`, `autoResolve=false`, SQL-null IP allowlist, and a matching stored key hash. Codex connection `8f92f200-d280-47b3-b380-2d94be0a4b75` must remain inactive. The store must remain an exact root-owned `0600` regular no-symlink file under the reviewed root-owned `0700` directory.

## Action-time preconditions and isolation

Before stopping the candidate, the remote preflight requires the exact live container ID, R4 ID/image/tag/source, running healthy state, restart count zero, user `node`, workdir `/app`, read-only root, restart `no`, exact noexec tmpfs, all capabilities dropped, `no-new-privileges`, network-none, zero ports, sole exact local volume mount, stable `server.env`, `cloudEnabled=false`, Codex inactive, the complete key policy, migrations `('170','agent_route_runs')` and `('171','agent_route_deferred_metrics')`, nullable deferred column, and unchanged detail-log baseline. Candidate and companion environments must equal the exact pinned image environment plus the finite reviewed runtime overrides; any extra name or changed value fails. It also requires the exact existing network and live/Q4/team peers and the selected Q4 row's exact provider, `apikey` auth type, enabled state, API-key credential with null access token, base URL `http://bell-cloudflare-proxy:20129/v1`, and no extra-key rotation list. It decrypts the API key with the native static-salt format when encrypted, preserves native plaintext compatibility, keeps the value in memory, and performs one bounded authenticated `GET /v1/models` through the Q4 address derived from matching Docker network metadata. HTTP 200 and exact upstream model `qwen3.8-27b-unsloth-ud-q4ks` are mandatory **before** any candidate stop. Any credential, HTTP, schema, model, address, or topology mismatch stops without mutation; no credential value or response body reaches output or evidence.

Only after all checks pass may the gate stop the exact candidate, prove it stopped, and create one fresh companion `omniroute-auto-switch-hardware-r1-20260913` on the existing private network with the same private volume. The companion uses the same pinned image, user `node`, no ports, read-only root, exact noexec tmpfs, all capabilities dropped, no-new-privileges, restart `no`, one volume mount, exact nonsecret disable environment, and bounded native health accounting. The key is read only on VM1205 through stable no-follow file descriptors and sent only through Docker-exec stdin; it never enters argv, Docker environment, Windows output, logs, or receipts.

## Hardware proof

The batch first requires authenticated VM1201 aggregate metrics `processing=0,deferred=0`. It starts two unique `agent/normal` tuples concurrently and samples until the same independently authenticated Q6 signal is exactly `processing=2,deferred=0`; each completed/failed future is resolved before classification so transport uncertainty cannot become a normal saturation failure. Local metrics and request envelope durations use one Windows monotonic clock, proving the third request starts only after that sample completes. The third tuple's persisted SQLite timestamps use one VM1205 database clock and must prove Q6-full before Q4 dispatch and Q4 selection no more than 5000 ms after turn creation. It must record zero admitted Q6 submissions, one exact Q4 submission, selected Q4 HTTP 200 with nonempty valid content. The first two tuples must each use exact Q6 once. All three require candidate attempt 1, exact provider/model/connection, objective PASS, output started, reviewer class/model/verdict/attempt, completed turn, ordered timestamps, and no replay or extra dispatch. Final metrics must return to `0/0`.

The companion is then stopped exactly once and retained. Every request sends its UUID as the caller `X-Correlation-Id` and requires the bounded chat response to echo that exact value before the UUID enters the result. SQLite opens the stopped volume with URI `mode=ro`, `query_only`, `quick_check` and verifies the three exact task/run/turn/idempotency tuples, key ID, event order, exact Q6/Q4 and reviewer identities, attempts, objective/reviewer PASS, the Q6 full and `processing=2,deferred=0` correlation, Q4-after-full ordering, and unchanged request-detail count. Together the response echo and persisted tuple bind each result request ID to the accepted runtime request. It rejects payload/detail logging and retains only hashes, IDs, counters, timestamps/durations, fixed categories, public model/provider/connection identifiers, verdicts, and booleans.

## Failure and evidence boundary

All local and nested child processes use the hash-pinned concurrent bounded capture helper and normal Windows environment with `PROGRAMDATA`; `PYTHONOPTIMIZE` is absent. A validated pre-mutation mismatch records STOP. Any timeout, overflow, stderr, malformed envelope, or uncertain child after candidate stop, companion creation/start, request dispatch, evidence stop, or final stop records UNKNOWN with nullable state, performs no retry/fallback/restoration/cleanup, and never claims the candidate or companion stopped. Only a known companion state may receive the contract's one exact stop. The local fixed result is reserved with `O_EXCL`, size bounded, fsynced, and closed. No live volume/service, client default, gateway, DNS, provider configuration, key, network, or old candidate is modified beyond this isolated reviewed batch.

Preparation verification is limited to the focused hardware test, launcher self-check, Python compilation, and diff checking. These checks execute the rendered credential helper only with synthetic values and perform no SSH, Docker, provider, credential-store, database, or network action. Passing them proves source preparation, not hardware qualification.
