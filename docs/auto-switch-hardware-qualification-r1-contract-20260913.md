# Actual-hardware qualification R1 source contract — 2026-09-13

**Status: PREPARATION ONLY / NOT EXECUTABLE.** `execution_ready=false`. This revision contains the fixed evidence schema and bounded request/metrics building blocks, but it cannot contact SSH, Docker, a provider, a credential store, or a network. The spent R4 startup receipt is pinned only as `UNKNOWN`; it is never treated as PASS. A future revision must bind the fresh independently accepted wrapped `auto-switch-candidate-reconcile-r1-result/v1` receipt and exact current container ID plus a separately reviewed, fresh R4 candidate-only qualification key receipt, key ID, and protected root-store path. The reconciliation receipt must itself remain explicit that environment, database, native health, and historical startup were not checked; this hardware launcher revalidates those prerequisites independently. That future revision requires a new independent Sol High review, reviewed pins, read-only preflight receipt, and explicit sole-owner dispatch. Old R1–R6 gates and the R3 candidate key must never be invoked or reused.

## Frozen source and dependencies

The target is source `e53d895e9a5e38a7f06ce59de254835f10e829c1`, image `sha256:8211e1071a3b68eac01673d76129150eb0c0fea329dd222bc8bf0394b13fc844`, tag `omniroute-auto-switch-r3:e53d895e9a5e38a7f06ce59de254835f10e829c1`, fresh R4 container `omniroute-auto-switch-candidate-r4-20260913`, and volume `omniroute-auto-switch-candidate-data-r4-20260913`. Pin the final proof plan `c8d6710b74f61e453e61a84c2af7836a500f3425a7252be2d816ed37d5a4d3bc`, bindings `c30aa56eba04d15a50c766a20ba44e88792c60bcddbdf25670fafd5955286e58`, provider R6 source `da1b1114d9273d95499604afff8e1a3de27750f174ff65a6593b219758278868`, authenticated metrics source `4fb6a77ef4ace419d4ab5094bbd6bfbdb276adfac2f0fe064332fb86923e343a`, bounded helper `78bb97e57d6229aae84c2f0d0caaf68eeb71ba6b92e69415d83af2a869b8224a`, and corrected candidate launcher `8dce8544eac30a163a520956a7ed6ee042f52bf00e769554c7d3e72d4fb33fab`.

The fresh R4 clone cannot contain the earlier candidate-only qualification key because the live source volume predates it. Key provisioning is a separate prerequisite, not part of this hardware batch. It must use the R4 native management path while R4 is isolated, create a new key restricted to exactly the original 14 models, five ordered connections, `agent:route`, endpoints `chat` and `models`, `noLog=true`, `autoResolve=false`, and store it in a fresh root-owned `0700` parent / exclusive `0600` regular no-symlink file. Its result must prove exhaustive native readback and bind the new key ID/store without exposing the value. This launcher must never accept the R3 key ID, store, or receipt.

## Required action-time preconditions

Before any future mutation, an extracted read-only preflight must revalidate the exact spent R4 UNKNOWN receipt without treating it as startup success; a fresh accepted reconciliation receipt; exact R4 ID/image/source label; running, healthy, restart-zero, network-none and zero-port state; sole exact local volume; `cloudEnabled=false`; Codex inactive; the fresh key policy; migrations `170_agent_route_runs` and `171_agent_route_deferred_metrics`; nullable integer `deferred_requests`; exact internal network/live/Q4/team peers; Q4 healthy and reachable; VM1201 exact container/image/config/bind/parallel-two/metrics/key-file identity; idle authenticated gauges `processing=0,deferred=0`; fresh companion and result absence; and unchanged live volume/client/DNS state. Any mismatch stops before candidate stop, companion creation, key read, metrics HTTP, or inference.

A reviewed consuming revision may stop only the exact R4 container, confirm it stopped, then create one fresh hardened companion `omniroute-auto-switch-hardware-r1-20260913` on the existing private network with the R4 volume. Use user `node`, no ports, read-only root, bounded noexec tmpfs, all capabilities dropped, no-new-privileges, restart `no`, the exact nonsecret disable environment, and no extra mounts. Read the new qualification key only from the stable no-follow root file into remote memory and a native `docker exec -i` stdin; never place it in argv, Docker environment, Windows, logs, or evidence. Every local and nested child uses the hash-pinned concurrent bounded helper and normal inherited Windows environment with `PROGRAMDATA` present.

## One minimum batch

Use three fresh task/run/turn/idempotency/request tuples and exactly three `agent/normal` calls. Start calls one and two concurrently, with no request retry. Require each to make exactly one Q6 submission and return a valid completion with the exact Q6 connection. Do not start call three until an independent authenticated VM1201 metrics sample reports exactly `requests_processing=2` and `requests_deferred=0`; bounded observation polling may stop on the first exact sample but must not generate traffic. If the sample is not observed inside 120 seconds, stop as NOT PROVEN.

Start call three while that sample is current. Require its persisted Q6 result event to show `admission_state=full`, `processing_requests=2`, `deferred_requests=0`, zero Q6 submissions, and Q4 selection within 5000 milliseconds from the candidate turn receipt. Require exactly one Q4 submission and HTTP 200 valid Q4 completion. HTTP 530/1033, any free/provider fallback, noncompletion, a second dispatch, invalid content, or missing timing/event correlation stops the gate; there is no waiver. Let calls one and two complete, then require authenticated VM1201 metrics to return to `processing=0,deferred=0`.

Stop and retain the exact companion once, then query the stopped volume with SQLite URI `mode=ro`, `query_only`, `quick_check`. Correlate the three exact tuples with completed turns and allowlisted route-event fields only: provider/model/connection, attempts, admission state, fallback reason, latency, busy/processing/deferred counters, objective outcome, reviewer class/model/verdict, event kind and timestamp. Require candidate/reviewer accounting, objective PASS, reviewer PASS, no payload/detail logging, and the exact key ID. Retain only hashes, IDs, counters, timestamps/durations, fixed categories, public model/provider/connection identifiers, verdicts and booleans. Never retain prompts, completions, reasoning, cookies, headers, token values, raw metrics, raw provider errors, database credential columns, or arbitrary logs.

The result must reject stale or wrong candidate/key/helper/source identity, any third Q6 submission, Q4 status other than 200, invalid content, dispatch after 5000 ms, malformed/noninteger/duplicate metrics, missing deferred correlation, missing tuples, unexpected attempts, or a companion that cannot be proven stopped. A transport or output-bound uncertainty is terminal and spent; it records UNKNOWN without claiming remote stop and performs no retry, fallback, diagnosis, restoration, or cleanup. A validated failure records the last proven candidate state and may perform only the contract's single exact companion stop and retain. Candidate and companion health evidence includes bounded attempts, timeouts, nonzero exits, and exactly one success, with `attempts=timeouts+nonzero+successes`; a result that breaks this accounting is rejected. No live volume, live service, client default, gateway, DNS, provider configuration, key, or network is modified by this batch.

Preparation verification is limited to `python -B scripts/test-auto-switch-hardware-qualification-r1-20260913.py`, launcher `--self-check`, Python compilation, and diff checking. These paths perform no SSH, Docker, HTTP, credential, provider, database, container, or network action. Passing them proves source preparation only.
