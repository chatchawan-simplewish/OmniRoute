# Retained candidate image transfer R2

Preparation only. This replacement has no execution authority until one independent review pins the exact JSON emitted by `scripts/auto-switch-image-transfer-r2-20260912.py --render-review-payload`; the sole execution owner must supply that unchanged JSON through `--execute-reviewed`. No command has been invoked by this preparation.

The procedure reads only the retained builder archive `/var/tmp/omniroute-auto-switch-image-transfer-20260912/image.tar`. It never calls `docker save`, does not export, delete, or clean up the spent root. It pins source `dc53bcfed67b3bbea7d2fbf82468342e573dadb3`, IID `sha256:a91994bf883698d4520ff048d16be999d4614331a528c76c0b95bc6dc8bec803`, tag `omniroute-auto-switch-r2:dc53bcfed67b3bbea7d2fbf82468342e573dadb3`, the spent launcher hash `7776a680ade8940ece3a47dc5feb154e0ef27209a07897674d309ea6f7822279`, and frozen OCI-helper SHA-256 `6bf5248b2859b87bc422784342751aa7b91ae95ed530039e7da6f11b6c7eca01`.

Before any copy, it hashes the retained archive and uses the pinned OCI helper against its manifest/config/descriptor graph. It accepts either a config identity or a bounded OCI descriptor identity, then requires the same positive byte count and SHA-256 after download and after upload. New paths are `scripts/auto-switch-image-transfer-r2-20260912` locally and `/var/tmp/omniroute-auto-switch-image-transfer-r2-20260912` on VM1205; both must be absent, non-symlinked, and private before mutation. The candidate IID and tag must be absent on VM1205 before load. After load, the exact tag resolves to the accepted IID and the existing live `omniroute` container remains pinned to its current image and running state.

Capacity checks cover `/var/tmp`, DockerRootDir, and, when Docker reports the containerd snapshotter, the active containerd root parsed from the sole running containerd process' explicit config and that config's `root` metadata. Docker documents that Engine 29's containerd store may be separate from DockerRootDir: https://docs.docker.com/engine/storage/containerd/ . Missing, ambiguous, or non-metadata-derived containerd configuration stops before transfer; no default path is assumed.

SSH/SCP use the existing strict host-key and dedicated identity convention. Each subprocess has a bounded timeout. The only failure envelope contains status, stage, exception class, and `TIMEOUT` or `CHECK_FAILED`; it suppresses command output and raw stderr. Any failure, uncertainty, pin drift, or mismatch stops without retry, cleanup, service start, deployment, or claim of transfer success.
