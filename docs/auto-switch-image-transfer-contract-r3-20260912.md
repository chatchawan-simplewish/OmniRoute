# Retained candidate image transfer R3

Preparation only. This replacement has no execution authority until one independent Sol High review pins the exact JSON emitted by `scripts/auto-switch-image-transfer-r3-20260912.py --render-review-payload`; the sole execution owner must supply that unchanged JSON through `--execute-reviewed`. No command has been invoked by this preparation. R2 is spent and is never retried.

The procedure reads only the retained builder archive `/var/tmp/omniroute-auto-switch-image-transfer-20260912/image.tar`. It never calls `docker save`, does not export, delete, or clean up the spent root. It pins source `dc53bcfed67b3bbea7d2fbf82468342e573dadb3`, IID `sha256:a91994bf883698d4520ff048d16be999d4614331a528c76c0b95bc6dc8bec803`, tag `omniroute-auto-switch-r2:dc53bcfed67b3bbea7d2fbf82468342e573dadb3`, the spent launcher hash `7776a680ade8940ece3a47dc5feb154e0ef27209a07897674d309ea6f7822279`, and frozen OCI-helper SHA-256 `7f822582a2ef5ce6c515ba002c7523b787cc3703e255ce5ec1cd0dca6bfc662a`.

Before any copy, the builder only proves the retained archive is a positive-size regular non-symlink file and returns its SHA-256. It does not inspect Docker, capacity, containerd, processes, or configuration on the builder. The pinned OCI helper then validates the archive manifest/config/descriptor graph. The procedure requires the same positive byte count and SHA-256 after download and after upload. New paths are `scripts/auto-switch-image-transfer-r3-20260912` locally and `/var/tmp/omniroute-auto-switch-image-transfer-r3-20260912` on VM1205; both must be absent, non-symlinked, and private before mutation. Local capacity and VM1205 `/var/tmp`, DockerRootDir, and metadata-derived containerd capacity remain required. The candidate IID and tag must be absent on VM1205 before load: each inspect must return only the exact allowlisted missing-image message as well as exit code 1 and `[]`. The two VM1205 phases use explicit `sudo -n docker`; builder Docker remains unprivileged. After load, the exact tag resolves to the accepted IID and the existing live `omniroute` container remains pinned to its current image and running state.

Capacity checks cover `/var/tmp`, DockerRootDir, and, when Docker reports the containerd snapshotter, the active containerd root parsed from the sole running containerd process' explicit config and that config's `root` metadata. Docker documents that Engine 29's containerd store may be separate from DockerRootDir: https://docs.docker.com/engine/storage/containerd/ . Missing, ambiguous, or non-metadata-derived containerd configuration stops before transfer; no default path is assumed.

SSH/SCP use the existing strict host-key and dedicated identity convention. Each subprocess has a bounded timeout. The failure envelope contains only a fixed stage and detail label, exception class, and `TIMEOUT` or `STAGE_FAILED`; it suppresses command output and raw stderr. Any failure, uncertainty, pin drift, or mismatch stops without retry, cleanup, service start, deployment, or claim of transfer success.
