# Retained candidate image transfer R4

Preparation only until independent Sol High review pins the exact JSON emitted by `scripts/auto-switch-image-transfer-r4-20260912.py --render-review-payload`. R1, R2 and R3 remain failed/spent and unchanged. Root owns this replacement implementation; the independent Sol High reviewer is the sole execution owner.

First run `--preflight-reviewed` with those pins. This is a read-only observation mode: it returns before any local or remote directory creation, download, upload or image load. It does not consume the transfer invocation. A passing result must be retained before the separate `--execute-reviewed` invocation. Execution repeats action-time checks and then may run exactly once; any execution failure stops without retry, fallback or cleanup. Changed source or contract bytes require new review; old transfer invocations are never reused.

The procedure reads only the retained builder archive `/var/tmp/omniroute-auto-switch-image-transfer-20260912/image.tar`. It never calls `docker save`, does not export, delete, or clean up the spent root. It pins source `dc53bcfed67b3bbea7d2fbf82468342e573dadb3`, IID `sha256:a91994bf883698d4520ff048d16be999d4614331a528c76c0b95bc6dc8bec803`, tag `omniroute-auto-switch-r2:dc53bcfed67b3bbea7d2fbf82468342e573dadb3`, observed archive size `1117870592`, observed archive SHA-256 `ef45e7734e64280cbe9ca9bbbd8a950f350c028c50e9b02c55ab0e48801abb15`, the spent launcher hash `7776a680ade8940ece3a47dc5feb154e0ef27209a07897674d309ea6f7822279`, and frozen OCI-helper SHA-256 `7f822582a2ef5ce6c515ba002c7523b787cc3703e255ce5ec1cd0dca6bfc662a`.

Before any copy, the builder proves the retained archive is a regular non-symlink file whose size and SHA-256 equal the pinned observation. A mismatch stops before OCI validation or copy. No builder Docker, capacity, containerd, process or configuration inspection is needed. The pinned OCI helper validates the archive manifest/config/descriptor graph. The same size and SHA-256 must match after download and upload. Fresh paths are `scripts/auto-switch-image-transfer-r4-20260912` locally and `/var/tmp/omniroute-auto-switch-image-transfer-r4-20260912` on VM1205; both must be absent and non-symlinked before creation. The target parent is created mode 0700 and revalidated for owner/mode before load. Local capacity and target `/var/tmp`, DockerRootDir and actual containerd storage capacity are required. Candidate IID/tag must be absent before load, using image listing and exact native missing-image responses. Target Docker calls use `sudo -n`. After load, the exact tag resolves to the accepted IID with the single expected RepoTag; live `omniroute` ID/image/running pins must still match.

Containerd checks reuse the exact REMOTE literal from `scripts/auto-switch-containerd-root-diagnosis-20260912.py`, SHA256 `8e34792fa2a91297e5154589fca519c38f71123470aeaedc2cf50eb0b016ae2e`. The independently accepted result is pinned at SHA256 `10310a6939409723af636e4088378443dc3ac423d02fd2f02ecbaf8e10cb43ca`. The old diagnostic launcher/pins are not invoked. R4 includes its reviewed read-only logic as a new action-time check: native configuration plus explicit root override must match running metadata-DB FD evidence for every bounded stable daemon; all six checks and all native return codes must pass. Paths, argv, config contents and stderr stay in remote memory. No default storage path is assumed. This covers the separate containerd store described in [Docker's documentation](https://docs.docker.com/engine/storage/containerd/).

SSH/SCP use the existing strict host-key and dedicated identity convention. All subprocesses are bounded; native load allows 1800 seconds inside a 2400-second outer target-load bound. Failure output contains only static stage/detail labels and exception class, suppressing command output and stderr. No service start, candidate startup, provider request, secret access or client change is authorized. A transfer PASS establishes image availability only.
