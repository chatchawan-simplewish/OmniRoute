# Q4 R9 isolated qualification successor contract

Status: **SOURCE ONLY / NOT REVIEWED / NOT EXECUTABLE.** This is a new Q4 qualification gate, not a retry or authority claim for any historic invocation. It permits no SSH, Docker, credential, provider, traffic, state, gateway, or client-default action.

## Fresh leaves and direct-byte approval

The only future leaves are distinct caller-supplied basename-only `q4-r9-*` state and terminal names under the reviewed secret parent, both absent before use and exclusive to R9. The action pins the safe relative secret filename and leaf root; helper resolves the supplied secret path and rejects any parent/name drift without reading or emitting the secret. The launcher reads its own bytes, helper, this contract, and focused test once; it hashes those retained bytes into an exact source manifest and carries those exact bytes with canonical action and approval bytes into the helper. A fresh independent `gpt-5.6-sol` high approval must pin the canonical action SHA-256 and every source hash. The action must exactly pin independently supplied remote runtime, host, candidate ID, image, command, collector, endpoint, Q4 model, connection, request, credential-root, and leaf-root pins. Launcher and helper require exact schemas and canonical bytes, rehash the retained bundle, and reject schema, source, action, reviewer, runtime, credential, or leaf drift.

## Single future action

The sole future executor supplies only the action-pinned runtime; helper compares its native manifest and runtime-source bytes to the action before reservation. Before a secret is read, the helper reserves a new fsynced `Q4_R9_UNKNOWN` state leaf with `gate_spent=true`; any existing state or terminal leaf refuses replay. It attempts one stop in `finally`, including when start acknowledgement is lost. It may send one secret only to the reviewed authenticated Q4 collector. The secret must never be logged, hashed, emitted, or put in argv/environment. There is no retry, fallback, Q6 request, historic-gate reference, cleanup, or live contact in this source package.

PASS requires canonical collector-produced native evidence bytes parsed by the helper and retained only as a SHA-256 in the terminal leaf: one authenticated health and models result bound to candidate/model, and one completion bound to candidate/model/connection with HTTP 200, request count one, nonempty request ID, and positive completion bytes, plus stopped retention. Boolean values are rejected for integer proof fields. Otherwise the immutable UNKNOWN/SPENT state remains the only authoritative result. The separate terminal leaf is published only for PASS and cannot replace or remove the state leaf.

## Focused source check

Run `python -B scripts/auto-switch-q4-r9-20260915-test.py`. It is offline and uses a synthetic runtime/secret only. A fresh Sol High source review, then a separate exact-action approval and sole-owner execution contract, are required before any remote contact.
