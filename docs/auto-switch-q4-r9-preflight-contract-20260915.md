# Q4 R9 non-consuming descriptor preflight and action renderer

Status: **SOURCE ONLY / NOT REVIEWED / NOT EXECUTABLE.** This additive contract resolves the original R9 preparation contradiction; it does not amend or authorize the reviewed consuming R9 source.

After an independent Sol High review approves the exact review payload, one invocation may make exactly one bounded SSH connection to `belladmin@192.168.1.68`, using the pinned identity-file path, strict pinned `known_hosts`, no SSH configuration, and one connection attempt. The launcher retains the reviewed bootstrap bytes once and sends them only through stdin to `sudo -n /usr/bin/python3 -I`. There is no retry or fallback.

The retained POSIX bootstrap opens only `/root/.omniroute-qualification` and basename `auto-switch-candidate-qualification-r5-20260913.key` with `O_NOFOLLOW`, then uses `fstat` to return device, inode, owner, mode, size, and mtime metadata. It checks only the absence of the two named R9 state/terminal leaves. It never reads or hashes credential bytes, never runs Docker, never makes HTTP/provider traffic, and never creates, changes, reserves, or deletes a remote object.

The local launcher reserves one fresh receipt with `O_EXCL` before contact. PASS requires empty stderr, canonical exact-schema stdout, the fresh nonce, current timestamp, retained-bootstrap hash, exact host/path/leaf pins, root-owned `0700` parent, root-owned `0600` regular credential of 1–4096 bytes, and both leaves absent. Any timeout, overflow, nonzero status, malformed output, drift, or uncertainty writes only a local UNKNOWN receipt and forbids retry; neither PASS nor UNKNOWN is a consuming R9 state reservation.

PASS permits only the offline renderer to combine the receipt with an explicitly supplied private candidate IPv4 and UUIDv4 request ID. It materializes exact runtime, command, collector, request, canonical action, and receipt-bound exact-path review-package bytes with `O_EXCL`; the action binds the already reviewed R9 launcher/helper/contract/test bytes, all four rendered operational bytes, host, candidate, image, endpoint, Q4 model, Q4 connection, timeouts, credential descriptor identity, and leaf names. The supplied templates are real later-action programs but remain unreviewed and must not be run before a fresh exact-action Sol High review, root-POSIX offline test, absent-leaf revalidation, and sole-owner consuming authority.
