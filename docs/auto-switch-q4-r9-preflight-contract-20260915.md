# Q4 R9 non-consuming descriptor preflight and action renderer

Status: **SOURCE ONLY / NOT REVIEWED / NOT EXECUTABLE.** This additive contract resolves the original R9 preparation contradiction; it does not amend or authorize the reviewed consuming R9 source.

After an independent Sol High review approves the exact review payload, one invocation may make exactly one bounded SSH connection to `belladmin@192.168.1.68`, using the pinned identity-file path, strict pinned `known_hosts`, no SSH configuration, and one connection attempt. Approval validation retains the reviewed `known_hosts` bytes once. The launcher writes those exact bytes to a fresh private temporary file, makes it read-only, holds a Windows read handle that denies write/delete sharing, and gives only that retained path to SSH. It verifies the held object identity, metadata, and exact bytes before cleanup; the exact file and directory must then be removed. Replacement, mutation, verification, or cleanup uncertainty leaves UNKNOWN/SPENT and forbids retry. The launcher likewise retains the reviewed bootstrap bytes once and sends them only through stdin to `sudo -n /usr/bin/python3 -I`. There is no retry or fallback.

The retained POSIX bootstrap opens only `/root/.omniroute-qualification` and basename `auto-switch-candidate-qualification-r5-20260913.key` with `O_NOFOLLOW`, then uses `fstat` to return device, inode, owner, mode, size, and mtime metadata. It checks only the absence of the two named R9 state/terminal leaves. It never reads or hashes credential bytes, never runs Docker, never makes HTTP/provider traffic, and never creates, changes, reserves, or deletes a remote object.

The local launcher reserves one fresh receipt with `O_EXCL` before contact. PASS requires empty stderr, canonical exact-schema stdout, the fresh nonce, current timestamp, retained-bootstrap hash, exact host/path/leaf pins, root-owned `0700` parent, root-owned `0600` regular credential of 1–4096 bytes, and both leaves absent. Any timeout, overflow, nonzero status, malformed output, drift, cleanup failure, or uncertainty writes only a local UNKNOWN receipt and forbids retry; neither PASS nor UNKNOWN is a consuming R9 state reservation. Once the receipt exists, every top-level failure reports authoritative `UNKNOWN` with `preflight_spent=true`; `NOT_EXECUTED` is permitted only when no receipt was reserved and therefore no contact can have begun.

PASS permits only the offline renderer to combine the receipt with an explicitly supplied private candidate IPv4 and UUIDv4 request ID. It materializes exact runtime, command, collector, request, canonical action, and receipt-bound exact-path review-package bytes with `O_EXCL`; the action binds the already reviewed R9 launcher/helper/contract/test bytes, all four rendered operational bytes, host, candidate, image, endpoint, Q4 model, Q4 connection, timeouts, credential descriptor identity, and leaf names. The runtime derives the candidate IPv4 from Docker inspect on the exact pinned `omniroute-internal` network and requires it to equal the reviewed endpoint before success. After the one completion, the collector opens the exact retained candidate SQLite database read-only and queries the exact server-returned OmniRoute log ID; PASS requires one row whose status, model, provider, and directly observed selected connection equal the reviewed pins. Evidence uses that observed connection value and never copies it from the action. The supplied templates are real later-action programs but remain unreviewed and must not be run before a fresh exact-action Sol High review, root-POSIX offline test, absent-leaf revalidation, and sole-owner consuming authority.
